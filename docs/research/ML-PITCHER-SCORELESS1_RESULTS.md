# ML-PITCHER-SCORELESS1 交付：投手連續無自責分局數（保守下界）

- 卡面：[`docs/tasks/ML-PITCHER-SCORELESS1.md`](../tasks/ML-PITCHER-SCORELESS1.md)　spec 基線 v1
- 口徑單一來源：`src/cpbl/models/scoreless_streak.py` 的模組 docstring（本檔不重述數字，只放結果與對帳）
- 取數層：`src/cpbl/api/scoreless.py`／端點 `GET /api/v1/records/earned-run-free-streak`
- 對帳腳本：`scripts/reconcile_scoreless_streak.py`（本檔所有對帳數字由它產生）

## 1. 做了什麼（以及刻意沒做什麼）

**沒有重建自責分。** 規則 9.16 的三個障礙（反事實重播、「有疑慮時對投手有利」的裁量條款、
繼承跑者按人數歸屬）寫在卡面背景，本次實作全程遵守：全檔案找不到任何失誤／捕逸／出局機會
的自行判定，自責分一律讀官方 `cpbl.pitching_gamelog.earned_runs`。

實際做的是**定位**，只用兩種事實：

1. 出賽的官方 `earned_runs`（判斷這場乾不乾淨）。
2. 逐打席 `game_livelog` 的**半局得分**（判斷中斷那場的哪些半局零得分）。

第 2 點值得強調：採計尾段半局的判準是「**整個半局、不分投手、零得分**」。半局零得分
⇒ 沒有任何分數存在可被判給任何投手 ⇒ 對本投手零自責分。這比「該場官方 ER=0」更強
（連非自責分都沒有），而且完全不需要知道自責／非自責的分野。

## 1.5 紅線 3 的字面與意圖（需求方裁定 2026-07-28，查核者請先讀這段）

紅線 3 寫的是「凡本卡宣稱無自責分的每一局，**其所屬出賽**的官方 `earned_runs` 必為 0」，
粒度是**出賽層級**。尾段採計的局屬於一場 ER>0 的出賽，字面上不滿足這句話。

需求方裁定：**尾段以半局層級的更強證明滿足紅線 3 的意圖，不是繞過它。**

| | 證明對象 | 證明內容 | 強度 |
|---|---|---|---|
| 紅線 3 字面 | 出賽 | 官方判定該場零自責分 | 該場沒有自責分，但可能有非自責失分 |
| 尾段判準 | 半局 | **整個半局、不分投手、零得分** | 連非自責分都沒有 ⇒ 沒有任何分數存在可被判給任何人 ⇒ 對本投手零自責分 |

半局層級的證明**更緊**，且同時繞開自責／非自責分野與 9.16(g) 繼承跑者歸屬（跑者不跨
半局，所以判給某投手的分必定發生在他投過的半局內）。兩者各有獨立的窮舉對帳：出賽層級
走 **R1**、半局層級走 **R2**（且 R2 用兩個獨立來源驗證），皆零例外。

因此 payload 的 headline 是 `outs` / `innings`（含尾段），`strict_*` 保留為紅線 3 字面
粒度的稽核值。只給 strict 不可行：先發投手第一局掉一分就歸零（黃子鵬 7/26 後
`strict=0.0`、實際 5.0），那不是「連續紀錄」這個詞的意思。

## 1.6 賽別範圍（需求方裁定 2026-07-28）

**只計例行賽局數**（一軍 A／二軍 D），不沿用 `KIND_GROUPS` 把季後賽併入同層。理由：
MLB／NPB 慣例即連續紀錄只算例行賽，使用者手邊的媒體數字也是這個口徑。

跨季時中間的季後賽出賽如何處理由執行者裁定，結論是**「乾淨跳過、掉分中斷」**：

| 季後賽出賽 | 處理 |
|---|---|
| 官方 `earned_runs = 0` | 跳過：局數不計入，也不中斷 |
| 官方 `earned_runs > 0` | 中斷（`break_reason = postseason_earned_run_allowed`） |

**為什麼不是一律跳過**（需求方原傾向一律跳過，以下是改為此案的理由）：

1. **紅線 2 的方向**。此規則下的值同時是「只算例行賽（季後賽全跳過）」與「一軍所有比賽
   都算」**兩種讀法的下界**，任一讀法下都不高估。一律跳過只在前一種讀法下成立，遇到
   第二種讀法就是高估——而紅線 2 要求的正是「寧可少報」。
2. **可理解性**。一律跳過會產生「這條連續紀錄橫跨一場他被打爆的台灣大賽」的輸出，
   正是需求方擔心的「讀者覺得怪」。實測 2018+ 一軍有 **25 位投手**的紀錄會落在這個
   情境（季後賽掉自責分），不是理論邊角。
3. **不變式好講也好驗**。此規則下「起算場之後、該投手在**任何**一軍賽別的出賽都沒有
   自責分」恆為真，是可窮舉驗證的強陳述（對帳 R7 逐人驗過）。

被跳過的場次以 `skipped_postseason_appearances`（數量）與 `skipped_postseason_games`
（場次清單）對外揭露，**不做沉默跳過**。實例——古林睿煬：2024 例行賽中斷後，
2024-10-12（E）與 2024-10-19（C）兩場季後賽皆 ER=0 被跳過，目前例行賽紀錄 3.0 局。

二軍側 `skipped = 0`：`pitching_gamelog` 2018+ 沒有任何二軍季後賽（F）出賽資料，
規則仍照樣套用，只是母體為空。

## 1.7 覆蓋完整性閘門（iteration 2 修正 F1）

查核 iteration 1 抓到一條**高估**路徑，這是本卡最重要的一次修正，根因值得記下來。

**問題**：`tail_credit` 反向走的是「**看得見的**」投手半局。若某個半局整段不存在於
livelog，它會被**跨過**而不是被看見——於是更早的乾淨半局仍被採計，而官方的自責分
可能正落在那個消失的半局裡。原本只辨識「**整場**沒有 livelog」，擋不住這種局部缺漏。

**根因是對帳的量詞方向**：R2 驗的是「**已被選入的**半局零得分」，證明的是**選中的都
乾淨**，不是**沒有漏掉的**。「零例外」因此無法排除這條路徑。檢查看得到的東西，
不等於檢查該成立的性質。

**修正**：新增 `coverage_reason()` 覆蓋完整性閘門，任一缺陷即 fail-closed（尾段回 0）：

| 代號 | 條件 |
|---|---|
| `duplicate_half_innings` | livelog 出現重複半局鍵 |
| `no_scoreboard` | 該場無 `game_scoreboard`，無從交叉驗證 |
| `livelog_half_missing_from_scoreboard` | 兩來源不一致 |
| `scoreboard_half_missing_from_livelog` | 獨立來源有、livelog 沒有，且非良性樣態 |
| `pitcher_half_innings_not_contiguous` | 該投手半局不是同一側連號一段 |
| `pitcher_outs_below_official` | 觀測出局數少於官方局數 ⇒ livelog 缺了他投的內容 |

並把 `game_scoreboard` 的零得分交叉驗證從對帳腳本**移進 runtime**：採計半局必須在
livelog 與記分板**兩邊**都零得分；`tail_credit` 的 `scoreboard` 是必要參數而非選配，
呼叫端無法略過這道檢查。

**良性樣態必須放行**（否則會把正常場次全數歸零）：實測 2018+ 有 **1,814** 個半局是
「主隊未進行的最終局下半」（記分板有列、livelog 沒有、零得分），另有 **11** 個是
記分板超出 livelog 範圍的空白列（皆 0 分 0 安打）。**DB 內不存在任何中間段的缺漏**
——所以修正前沒有已實現的錯誤，但那是運氣，紅線 2 要的是保證。

**兩個估計值方向相反，這是刻意的**：覆蓋比對用的 `pitcher_outs` 取**寬鬆上界**
（估低會誤擋正常場次），採計用的 `provable_outs` 取**嚴格下界**（估高會多報）。

### 變異檢驗（F1 修正的有效性證明）

對全部 343 個一軍尾段查詢，把「該投手最後一個有得分的半局」整段從 livelog 移除，
模擬缺漏：

| 指標 | 結果 |
|---|---|
| 基準有尾段的出賽 | 47 |
| 變異後尾段歸零（＝攔截成功） | **47 / 47** |
| 變異後仍有尾段（＝漏抓） | **0** |
| 攔截原因 | 全數 `scoreboard_half_missing_from_livelog` |

同一組變異下，**修正前**的行為會在 **116 個出賽**多報尾段，合計 **951 個出局數**、
單筆最多 **6.0 局**（例：羅力 2022/A/246 修正前 9 outs → 修正後 0）。修正後全數歸零。

對帳側同步新增 **R8 覆蓋完整性檢查**（獨立於 runtime 路徑重做判定，並用「投手半局數
× 3 ≥ 官方出局數」這個與 `_observed_outs` 無關的上界），140 個尾段出賽全數通過。

## 1.7b 半局**內部**完整性（iteration 3 修正 F1-b）

iteration 2 修的是「整個半局缺漏」，**沒修「半局內事件缺漏」**。查核者的反例：官方 4
outs；第 1 局失分半局取 3 outs；第 2 局零得分半局他只記 1 out 就換投，而**換投後的所有
事件都不在 livelog**。現存列仍全屬他、首列 `out_cnt=0` → `pitched_whole=True`；
`_observed_outs` 找不到後任投手就以 3 作上界，把缺漏蓋掉；半局集合比對通過。
實測高估 2 outs。

### 這是同一個病的第三層——值得記下來

| 層 | 我檢查了什麼 | 該檢查什麼 |
|---|---|---|
| R2 | 選中的半局零得分 | 有沒有半局漏掉 |
| 覆蓋閘門 | 半局是否存在 | 半局內是否完整 |
| `pitched_whole` | 現存列是否一致 | 列是否齊全 |

**每修一層就露出下一層，因為修法都是「再檢查一個看得見的量」。** 三次都用 livelog
自己說的話去驗 livelog。要跳出循環，證據必須來自 livelog **之外**。

### 修法：兩道證據，一近一遠

1. **局部直接證據**——採計整個半局需要看到「他投到第三個出局的那個打席」，即該半局
   最後一列 `out_cnt == 2`。原本 `last_out_cnt in (0,1,2)` 形同不檢查。
   實測：現行 101 個採計半局裡 98 個本來就滿足，3 個不滿足（正是無法證明的那些）。
2. **外部獨立證據**——新增 `GameEvidence.official_outs`：**全場每位投手**的官方出局數
   （`pitching_gamelog`），配合 `out_allocation()` 以**半局內的投手更迭邊界**推出每位
   投手的可見出局數區間。官方 box 知道有哪些投手、各記了幾個出局；livelog 若漏掉某位
   後援投手的事件，他的出局數就**沒有可見的位置可以安放**，對帳立刻不平。

`tail_credit()` 介面同時收成「原始事件列 ＋ `GameEvidence`」，解析與閘門都在函式內做，
呼叫端無法只餵半局清單而繞過證據檢查。

### 變異檢驗（F1-b）

對全部 47 個「基準有採計尾段」的出賽做兩種變異：

| 變異 | 結果 |
|---|---|
| M1：該半局只留下他的第一個打席（純事件缺漏） | 尾段**下降 47 / 47**，漏抓 0（由 `last_out_cnt == 2` 攔下） |
| M2：查核者反例——同上，且官方 box 顯示他該半局只記 1 out | 尾段**歸零 47 / 47**，漏抓 0，全數 `official_outs_outside_visible_range` |

### 這次數字真的降了，而且是在真實資料上

**新閘門在真實資料上會觸發**（不像 iteration 2 只有變異才觸發）：

| 層級 | 尾段查詢 | 通過 | `official_outs_outside_visible_range` | `pitcher_outs_below_official` |
|---|---|---|---|---|
| 一軍 | 343 | 341 | 2 | 0 |
| 二軍 | 489 | 481 | 7 | 1 |

被排除的 8 場（10 組投手×場次）都是**官方 box 與 livelog 出局數配置差 1 個出局**，
位置多在投手更迭邊界。例如 2026/A/158：賴鴻誠官方 2 outs 但可見區間 (1,1)、林栚呈官方
2 outs 但可見區間 (3,3)。這是真實的資料不一致，**證明不了就不採計**是正確處置。

採計半局因此下降：一軍 101 → **98**、二軍 166 → **160**（差額轉為「0 採計半局」，
不中斷連續紀錄，只是不貢獻出局數）。

**對產品沒有實質影響**：2026 一軍排行榜名次與數值完全未變，兩筆公開紀錄對照
（坎南 30.0、黃子鵬 33.2）也完全未變——被排除的都是榜外的零星場次。

## 1.8 端點值域（iteration 2 修正 F2）

`kind_code` 原本無值域限制，`C` 與不存在的 `Z` 都回 200，產生 `kinds_counted=['C']`／
`['Z']` 而 `scope_note` 仍宣稱「只計例行賽」——**回傳資料與自身宣稱矛盾**。

現以 `pattern="^(A|D)$"` 鎖死。**選 `^(A|D)$` 而非 `^A$` 的理由**：二軍紀錄同樣有效
且已完整對帳（對帳腳本本來就跑兩個層級，二軍 800 場採計出賽、166 個尾段半局全數零
例外），沒有理由在 API 層砍掉一個已驗證的層級；預設值與前端消費面仍是 `A`。
非例行賽賽別（C／E／F）與未知代碼一律 422。

## 1.9 資料邊界的實際執行（iteration 2 修正 F3）

查核抓到紅線 4 **根本沒有真的擋**：`DATA_FROM_YEAR` 只出現在 payload 的 `data_from_year`
顯示欄，取數 SQL 沒有 `year >= 2018`，核心函式也不檢查。查核者實測輸入 2017、2018 各
1 局 → 輸出 2.0 局。目前看不出問題純粹因為 DB 最早就是 2018——**跟 F1 同一個病：
運氣不是保證**。

而且既有的 `test_boundary_limited_when_all_appearances_consumed` 只驗 `boundary_limited`
旗標，**沒有驗「跨 2018 必截斷」**，是空泛替代測試：它會通過一個根本沒 enforce 邊界的
實作。這次補的 `test_pre_2018_appearances_are_truncated` 直接驗行為（2017 那場不得計入）。

**修正**：兩層都 enforce。

- 取數層：`_APPEARANCES_SQL` 加 `AND p.year >= %(from_year)s`
- 核心函式：`compute_streak(..., data_from_year=DATA_FROM_YEAR)`，回走遇到早於邊界年的
  出賽即截斷、`break_reason=data_boundary`、`boundary_limited=True`（不得沉默截斷）

核心函式那層是 fail-closed 的守門：呼叫端就算自己餵原始列進來也繞不過。

### 變異檢驗（F3）

在每位「已走完全部出賽」投手的序列最前面插入一場 2017 的乾淨出賽，模擬 SQL 過濾失效：

| 指標 | 結果 |
|---|---|
| 受測投手（`boundary_limited=True`） | 17 |
| 插入後被截斷（`break_reason=data_boundary`） | **17 / 17** |
| 插入後多算進去（＝洩漏） | **0** |

對帳 R6 同步補上「採計／跳過的出賽年份不得早於 `DATA_FROM_YEAR`」的逐人檢查。

## 2. 窮舉對帳（自動產生，非人工聲明）

重跑指令：

```bash
uv run python scripts/reconcile_scoreless_streak.py --json-out artifacts/scoreless-reconcile.json
```

母體＝`pitching_gamelog` 2018+ 的**每一位投手**（含二軍），對每人重算其目前連續紀錄，
再逐項回頭比對原始資料。任一例外 exit 1。以下為 2026-07-28 執行輸出（原樣貼上）：

```
窮舉對帳：連續無自責分局數（ML-PITCHER-SCORELESS1）

層級 | 投手數 | 出賽總數 | 採計出賽 | R1 驗得 ER=0 | 尾段採計半局 | R2 驗得零得分 | 尾段 0 採計半局 | R2 驗得零得分 | 跳過季後賽 | R7 驗得 ER=0 | 尾段出賽 | R8 半局覆蓋 | R9 出局數配置 | 例外
--- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | ---
一軍例行賽 A | 391 | 22307 | 476 | 476 | 98 | 98 | 14 | 14 | 7 | 7 | 53 | 53 | 53 | 0
二軍例行賽 D | 552 | 17627 | 800 | 800 | 160 | 160 | 25 | 25 | 0 | 0 | 86 | 86 | 86 | 0


總例外：0（紅線要求 0）
```

**紅線 3（凡宣稱無自責分的每一局，其所屬出賽官方 ER 必為 0）：943 位投手、1,276 場採計
出賽，重查 DB 後 1,276 場全數 ER=0，零例外。** 尾段 267 個採計半局全數經兩個獨立來源
驗得零得分；7 場被跳過的季後賽出賽全數驗得官方 ER=0。

> 賽別範圍改為純例行賽後的變動（vs 前一版 A/E/C 併計）：一軍採計出賽 483 → **476**、
> 尾段採計半局 120 → **101**、尾段 0 採計半局 13 → **11**，新增跳過季後賽 **7**。
> 二軍不變（無二軍季後賽資料）。2026 一軍排行榜名次與數值未變（榜上投手今年皆無季後賽出賽）。

各檢查項對應紅線見腳本 docstring 的表。要點：

- **R1** 不信任計算時的記憶體物件，以 `(year, kind_code, game_sno, pitcher_acnt)` 重查 DB。
- **R2** 用**兩個獨立來源**驗尾段半局零得分：`cpbl.game_scoreboard`（官網 box 的另一段
  payload）＋ livelog 側以 `lag()` 視窗重算（計算路徑用的是前綴最大值，兩者互為獨立實作）。
- **R4** 連續性：採計 ∪ 跳過的出賽必須恰好是該投手出賽序列的結尾連續段，由原始行獨立重建比對。
- **R7** 賽別範圍：採計必為例行賽、跳過必為非例行賽且官方 ER=0；並驗「起算場之後該投手在
  **任何**賽別的出賽都無自責分」這條強不變式。
- **R9** 事件／投手邊界粒度：以 SQL 視窗函數在半局內的投手更迭邊界重算出局數配置，
  驗全場每位官方 box 上的投手都落在自己的可見區間內。**R8 只比半局集合，與 runtime
  共享「半局存在即內部完整」的盲點；R9 是唯一能抓到半局內部缺漏的檢查。**

### 另一路獨立實作的交叉驗證

`strict_outs`（只由整場 ER=0 組成的部分）另以**純 SQL 視窗函數**重寫一次，與 Python 路徑
逐人比對，2026 一軍前 10 名完全一致（85/60/57/40/39/36/29/28/27/24 出局數，場次數亦一致）。

## 3. 與公開紀錄對照

**母體先講清楚**：CPBL「連續無失分」的公開紀錄榜前五名中，凱撒 45 局（2004）、風神
34.1 局（2000）、潘威倫 33 局（2005–06）都**早於本專案的 2018 資料邊界**，無法驗證。
落在 2018+ 且有媒體報導的連續紀錄只有**兩筆**：坎南 35 局與黃子鵬 33.2／34 局。
兩筆都已對照，結果如下（搜尋來源與嘗試過的管道見 §3.4）。

### 3.0 命中：坎南 35 局（聯盟史上第 2 高，**驗證尾段機制**）

媒體報導：台鋼雄鷹坎南（David Buchanan）**先發連續 35 局無失分**，改寫先發投手的
CPBL 紀錄（原紀錄風神 34.1 局），2026-06-26 對中信兄弟第 6 局遭王威晨二壘安打追平
而中斷；該場他**前 5 局無失分**。

即 6/26 開賽前他已累積 **30 局**。把資料截到 2026-06-20 跑本卡演算法：

```
截至 2026-06-20：30.0 局 = 整場 27.0 局（4 場：05-10、05-23、05-29、06-20）
                          + 尾段 9 outs（04-23 那場的 5、6、7 局上）
```

**完全吻合。** 這一筆比 §3.1 更有價值：30.0 局裡有 **3.0 局來自尾段**——他 04-23 那場
官方 ER=3，本卡靠「整個半局零得分」定位出該場最後三個乾淨半局。**公開紀錄獨立佐證了
尾段機制本身**，不只是整場累加。

紀錄中斷後（截至 2026-06-26）本卡輸出 **1.0 局**（該場第 7 局下），與「第 6 局中斷、
之後再投一局」一致。

### 3.1 命中：黃子鵬 33.2 局

媒體報導（2026-07）：黃子鵬自 6/7 起連 5 場、**33.2 局無失分**，7/26 對味全龍首局遭狙擊而中斷。

把資料截到 2026-07-15（報導當下）跑本卡演算法：

```
截至 2026-07-15：33.2 局（5 場：06-07 21 outs、06-13 27、06-21 18、07-08 17、07-15 18）
```

**與公開紀錄完全吻合**（賽別範圍改為純例行賽後重跑，數字不變——他這段期間無季後賽出賽）。

### 3.2 下界差距的實例（同一位投手）

7/26 那場的 livelog：他先記下 1 個出局，第 2 名打者被安打得分（第 1 局），之後投完第 1 局
剩下 2 個出局並續投 2～6 局。

- 真值（目前連續無自責分）＝第 1 局最後 2 個出局 ＋ 2～6 局 ＝ **5.2 局**
- 本卡輸出 ＝ **5.0 局**（`tail_half_innings=5`）

**差距 2 個出局**，來源是刻意的保守設計：得分半局的殘餘部分不採計（本卡只採計「整個半局
零得分」的半局）。這是紅線 2 要求的方向，寧可少報。

順帶驗證媒體的「斷在 34 局」：33.2 ＋ 該場第 1 局的 1 個出局 ＝ 34.0，數字對得上。

### 3.3 名詞紅線的實例：28.1 vs 9.0

呂彥青（2026-07-26 時點）：

| 口徑 | 值 |
|---|---|
| 連續無**自責**分局數（本卡） | **28.1 局** / 28 場 |
| 連續無**失分**局數（媒體常用口徑） | 9.0 局 / 9 場 |

差距來自 2026-06-11 那場：`runs=1, earned_runs=0`（失誤造成的非自責失分）。本卡口徑**不中斷**，
媒體口徑中斷。**把 28.1 標成「連續無失分」會是明確的錯誤**——這正是紅線 5 存在的理由。
端點回應永遠帶 `note` 說明兩者不同，欄位與文案一律寫「無自責分」。

> 註：這一節是**內部資料算出的口徑差異**，不是第三筆公開紀錄對照。公開對照只有
> §3.0 與 §3.1 兩位投手。

### 3.4 來源與搜尋範圍（可追溯）

公開紀錄來源：

- 坎南 35 局破紀錄與中斷：[聯合新聞網](https://udn.com/news/story/7001/9591288)、
  [TSNA](https://tsna.com/article/129204)、[三立新聞網](https://www.setn.com/News.aspx?NewsID=1862972)
- 黃子鵬 33.2 局與「斷在 34 局」：[芋傳媒](https://taronews.tw/2026/07/15/1203680/)、
  [中央社](https://www.cna.com.tw/news/aspt/202607080398.aspx)、
  [三立新聞網](https://www.setn.com/news/1878784)、[知新聞](https://www.knews.com.tw/news/2559411D73140322CB98773252982CAF)、
  [聯合新聞網](https://udn.com/news/story/7001/9630325)

找不到第三筆的原因（已嘗試的管道）：

| 管道 | 結果 |
|---|---|
| 中職連續無失分紀錄榜前五名 | 除上述兩位外，其餘（凱撒 2004／風神 2000／潘威倫 2005–06）皆早於 2018 資料邊界 |
| 台灣棒球維基館「例行賽個人記錄」頁 | 被 Anubis 反爬擋住，`WebFetch` 只取得錯誤頁（與記憶 `wiki-data-sources` 一致） |
| rebas.tw 球員頁 | SPA，`WebFetch` 取不到逐場資料 |
| 2018–2025 各年度「連續無失分」新聞搜尋 | 只找到 2025 季後挑戰賽一筆**季後賽 scope** 的「連續 4⅓ 局無自責分」，與本卡「只計例行賽」的口徑不同，不可直接對照 |

**這是現況陳述，不是自行豁免驗收條件**：卡面寫「抽驗**數名**投手」，目前公開對照為
**2 位投手、3 個檢查點**（坎南 06-20／黃子鵬 07-15 與 07-26）。是否足以滿足「數名」，
請需求方裁定；若需第三筆，需要放寬到季後賽 scope 或接受非媒體來源，兩者都需要需求方
明示。

## 4. 對外介面

`GET /api/v1/records/earned-run-free-streak?season=&kind_code=&player_id=&team=&limit=`

`kind_code` 是**計入局數**的例行賽賽別（預設 `A`）；同層季後賽仍在載入範圍內，只是不計
局數（乾淨跳過、掉分中斷）。回應以 `kinds_counted` / `kinds_in_scope` / `scope_note` 揭露。

每筆 item 的兩個值都是下界，差別只在對帳基礎：

| 欄位 | 組成 | 對帳 |
|---|---|---|
| `outs` / `innings`（**headline**） | 下者 ＋ 中斷場的「整個半局零得分」尾段 | R1 ＋ R2（半局層級的更強證明，見 §1.5） |
| `strict_outs` / `strict_innings`（稽核值） | 只有官方 ER=0 的整場出賽 | R1（紅線 3 字面粒度） |

另附 `boundary_limited` / `boundary_note`（紅線 4）、`break_reason`、`break_game`、
`skipped_postseason_appearances` / `skipped_postseason_games`（§1.6）、起算場與最近一場。

**為什麼兩個值都要留**：黃子鵬 7/26 之後 `strict=0.0`、`outs=5.0`。若只給 strict，先發投手
只要當場掉一分，紀錄板就歸零，那不是「連續紀錄」這個詞的意思；若只給 outs，就少了紅線 3
字面粒度的對帳落點。兩者並存，且兩者都有窮舉對帳撐著。

## 5. 已知的下界與真值差距（誠實揭露）

| 情境 | 少報多少 | 為什麼不修 |
|---|---|---|
| 得分半局的殘餘出局數 | 0～2 個出局 | 需要精確定位「最後一分之後」的出局歸屬；採計整個半局才有無爭議的證據 |
| 中斷場中他中途接手的乾淨半局 | 每個這種半局 0～2 個出局（不中斷，只是不採計） | 無法從 `out_cnt` 無爭議地切出他記下幾個出局；全母體 31 個半局命中此路徑 |
| 全場最後一個半局 | 至多 1 個出局 | 再見安打／保護傘可能未達三出局，只採計最後一列的打席前出局數 |
| 保留賽（`delay_kind='保留'`） | 該場全部 | 橫跨 orig_date→game_date，兩種排序都可能造成高估；2018+ 僅 8 場，直接中斷最乾淨 |
| ER>0 那場缺 livelog | 該場尾段全部 | 2018+ 僅 2019 年二軍 14 場出賽無 livelog |
| 跨 2018 之前 | 未知 | 資料邊界，以 `boundary_limited` 明示（紅線 4） |
| 季後賽掉自責分而中斷 | 該次之前的例行賽紀錄全部 | 賽別範圍裁定的刻意保守（§1.6）；2018+ 一軍 25 位投手落在此情境 |
| 官方 box 與 livelog 出局數配置對不上 | 該場尾段全部 | 半局內部完整性無法證明（§1.7b）；全母體 8 場、差額皆為 1 個出局 |
| 半局末列 `out_cnt != 2`（看不到第三個出局） | 該半局 3 個出局 | 證明不了他投完整個半局（§1.7b）；一軍 3 個、二軍 6 個半局 |

**差距會拉大的情境**：先發投手在**中局**掉自責分、之後續投多局——殘餘出局數與中途接手
兩條各吃掉 0～2 個出局。實務上正在追紀錄的投手近期多為 ER=0 的乾淨場次，下界即真值。

## 6. 邊界與未做

- 唯讀，不物化新表；`compute_all` 只對「ER>0 的那一場」抓 livelog，全季排行約 0.5 秒。
- 未做前端（消費方為 `UX-TEAM-RECORDS1`）。
- 未做歷史最長紀錄（本卡只算**目前**連續紀錄；歷史榜受 2018 資料邊界限制，另案）。
