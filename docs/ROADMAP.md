# CPBL 藍圖

> 需求方 2026-08-13／08-14 裁定的目標排序、規劃生命週期、任務線與卡片執行規範。
> **本檔是「為什麼做這張卡、現在做不做」的唯一依據**；卡片本身寫「怎麼做」。
> 開新卡前先在此找到它服務哪個目標與哪條線——找不到就不該開。
> **L5 的卡是明訂例外**：它答的不是「哪個目標」，而是**形式 A 解除的阻塞**或
> **形式 B 已發生的事故**；兩者都指不出來時答案是「急迫度最低且不得認領」（§1 L5）。
>
> **iteration 1 基線**：本版依 `DOC-CPBL-ROADMAP1` R1 跨家族查核＋需求方共同設計的
> **十項修訂基線**改寫，權威來源為 [#130 的 R1 共同設計附錄](https://github.com/ruan6047/cpbl-analytics/issues/130#issuecomment-5284078990)。
> 下文以「基線 N」引用該附錄，**不複述其原文**。

## 劃界（先讀，避免與既有文件重複或衝突）

| 文件 | 管什麼 | 本檔的關係 |
|---|---|---|
| [`PRODUCT_UX_BLUEPRINT.md`](PRODUCT_UX_BLUEPRINT.md) v0.2 **@ `6dab17b`** | **產品長什麼樣**：目標使用者、資訊架構、頁面決策 | 本檔不覆蓋、不複述。L3 的終點定義**引用**它的六項核心決策 |
| [`AI_RUNBOOK.md`](AI_RUNBOOK.md) | **怎麼操作**：指令、資料流、同步、陷阱 | 無交集 |
| `ai-workflow/docs/ROADMAP.md` **@ `52839f0`** | ai-workflow **自己**的目標與卡片分級 | 本檔 §4／§5 引用其對應節次，不複述 |
| `.ai-workflow/AI_WORKFLOW.md` **@ `71df157`** | 跨專案流程通則（階段、三級規劃閘門 §3.1、鏈式停損 §3.3） | 本檔在其之下，不牴觸；§2 是 CPBL 的**接線與加嚴**，不是替代 |
| [`incidents/INCIDENTS.md`](incidents/INCIDENTS.md) | 被本檔引用的事故錨點（時間、SHA-256、退出碼） | 本檔以 `INC-` 編號引用，**不複述細節** |

**複述會漂移。** 需要那些文件的內容時引用節次，不要把數字或條文抄過來。

---

## 0. 目標排序

需求方 2026-08-13 裁定：

| 序 | 目標 | 怎樣才算達成 |
|---:|---|---|
| 1 | **不出錯** | 對外呈現的數字有**對帳或機械檢查擋得住**。人工抽驗後宣稱正確**不算** |
| 2 | **不退化** | 已經在跑的東西壞掉時**會被發現**。「靠人記得看」不算——告警沒有讀者等於沒有告警 |
| 3 | **看得懂** | 球迷能在不讀方法論的前提下得到正確理解；進階讀者能展開依據 |
| 4 | **深度** | 分析能力的擴張。有最好，非必要 |

### 為什麼是這個順序

不是偏好，是這專案已經付出的代價（錨點見 [`incidents/INCIDENTS.md`](incidents/INCIDENTS.md)）：

- **目標 1**：分項重算誤把代打切成新打席，**83 筆／82 場**錯誤存在於線上，靠窮舉對帳才抓到（`INC-歷史`）
- **目標 2**：`INC-2026-08-10-A`／`-B` 兩個排程同日失敗、**三天無人知**；`INC-2026-08-13-A` 的 `D/97` 續賽後 PA 衍生表未重建，**四天無人知**；逐球設備覆蓋告警**響了兩個半月無人讀**
- 目標 3、4 至今**沒有造成過一次事故**

> ⚠️ 本檔 `0948bae` 版在此列了「`weekly-game-pitches` 排程交付後從未掛上」。**那是錯的**——
> `INGEST-GAME-TM-REFACTOR1-G4`（#53）載明週跑 plist 不安裝是 Phase A 的**刻意邊界**。
> 該誤述源自 `OPS-DORMANT-SCHEDULE-AUDIT1`（#115）的假前提，該卡已依 §2.6 停止。
> **論據換成實測案例，不是補一句免責。**

### 開卡前的三個檢查

1. 它服務哪一個目標？講不出來就不開。
2. 目標 1／2 的卡：**擋下它的是什麼？** 答案若是「靠人記得」，它其實是目標 3 或 4。
3. 目標 3／4 的卡：**現在有人因它受害嗎？** 沒有就進 `💡需求`，不排程。

> **L5（開發／文件基礎）的卡是第 1 問的明訂例外**（需求方 2026-08-14 裁定，見 §1 L5）
> ——它改答**形式 A**「解除哪一條線的哪一張卡的阻塞」或**形式 B**「防止哪一件**已發生**的事故再發」。
> 兩者都答不出來時，答案是**急迫度最低且不得認領**，**不是「無法判定」**。
> 第 2／3 問以目標分支，L5 的卡兩支都不觸發。

**這三問不是 Gate 本身。** Gate 是 §2.0 的規劃生命週期，通則見 `AI_WORKFLOW.md` §3.1。
三問只是「該不該進入規劃」的前置篩子。

---

## 1. 任務線

五條線，沿用 Project 既有的 **`initiative` 欄**（不發明新欄位）。歸屬由
[`scripts/roadmap_lines.py`](../scripts/roadmap_lines.py) 機械判定，**判不出來即 fail closed**——
新卡若前綴與明列表都不命中就會失敗，強迫作者做出判斷而非靜默落進預設線。

### 「何時算完」對五條線的意義不同 ⚠️

**只有 L3 有真正的終點。** 硬把終點套到其他線會產生假的完成宣稱，所以分三類：

- **狀態線**（L1、L2、L5）：終點是一個**可驗證的狀態**，不是一次性交付。「算完」＝該狀態成立**且有機制在它不成立時發出訊號**
- **有終點的線**（L3）：一份已定案的規格全數落地
- **無終點的線**（L4）：研究的產出是知識不是功能，**不設線的終點，改為每張卡自帶停損**

### L1 資料正確性（服務目標 1）

**算完的狀態**：對外呈現的每一個數字都有一條**可重跑的對帳路徑**，且該路徑當前零例外。

**訊號機制**：對帳腳本進 CI 或每日鏈；例外數 > 0 要有讀者。

> ⚠️ 這條線目前**不成立**：`INC-2026-08-13-A`（`D/97` 的 PA 衍生表未隨自癒重建，四天無人知）
> 已開卡為 `DATA-PA-REBUILD-GAP1`（#134），但**該卡的 Discovery 把受害面從 1 場修正為 7 場**
> ——其中 `D/119` 自 2026-08-08 起損壞，至今未被任何機制標示。**「可重跑的對帳路徑」
> 在 PA 這一層並不存在**，故本線的算完狀態不成立不是因為有一個待修的 bug，
> 而是因為**缺少發現它的手段**。詳見 §3。

### L2 每日鏈可靠性（服務目標 2）

**算完的狀態**：每日鏈的每一個步驟，「沒跑到」時都會被發現，**且該偵測機制本身被驗證過會響**。

**驗證方式**：刻意讓一個步驟失敗，確認訊號出現（fault injection）。沒做過這件事的偵測器不算數。

**`OPS-REMOTE-*` 子線的終點**：VPS 能在 exact allowlist 下安全爬 stats 域，**或明確判定不可行並關閉整條子線**。四張卡（probe → route → worker → cutover）全在 `💡需求`，**沒有人能說出它到哪算成功**——這正是本節要修的病。

### L3 產品／UX（服務目標 3）

**終點**：[`PRODUCT_UX_BLUEPRINT.md`](PRODUCT_UX_BLUEPRINT.md) v0.2 **§1「Executive Decision」的六項核心決策 @ `6dab17b`** 全數落地。**本檔不複述那六項。**

> 引用釘在 `6dab17b`（2026-08-01 最後修改）。該檔更新後，**先確認那六項仍是六項且語意未變**再沿用本終點定義；變了就要改這裡，不是默默繼續指過去。

這是唯一有明確終點的線，因為它的規格已經定案並查核過。

### L4 ML／研究（服務目標 4）

**不設線的終點。** 研究線的產出是「知道了什麼」，沒有完成度可言。

**改為逐卡停損（基線 4，硬性）**：

- **新研究卡進 `📥Backlog` 前**必須填出**可觀察的停止投資條件**
- **舊研究卡首次認領前**同樣必須補出
- **補不出來不得認領**——不是「先做做看再說」

**「可觀察」的判準**：條件要能由一次執行的輸出直接判定真假。
`train.py` 的「打不贏 Marcel baseline 就沒有價值」是合格範例——回測表印出來就知道。
**「效果不好就停」不合格**，因為沒有人能指出哪個數字算不好。

已知的停損實例：`WP-VAL1` 全 scope 判 unsupported、`CAL1` 與 `STRENGTH1` 兩條路徑皆 No-Go、`TEAM-STYLE2` 教練任期假說 No-Go。**這條線最有價值的產出是否證，不是功能。**

### L5 開發／文件基礎（跨線支援）

**算完的狀態**：文件與碼一致、CI 綠、無過期陳述。

**訊號機制**：`DOC-G4-FREEZE-STALE1`（#120）建立的全庫過期陳述掃描。

#### L5 不掛單一目標（需求方 2026-08-14 裁定）

L1–L4 各自標注「服務目標 N」，L5 標的是「跨線支援」——而 §0 的開卡第一問是
「它服務哪一個目標？**講不出來就不開**」。需求方裁定 **L5 是這一問的明訂例外**：

- **L5 不服務 §0 的單一目標**，它服務的是**其他四條線的目標達成率**
- **因此 L5 的卡不適用 §0 第一問**，其排序判準改為下列**兩種形式加一個 fallback**

**未採「每張 L5 卡自行宣告服務目標 1–4」的理由**：基礎建設幾乎都能宣稱自己「防止出錯」，
結果會是每張都掛目標 1，判準失去鑑別力——**那是把「答不出來」換成「答案沒有區別」，不是解決**。
**未採「L5 統一掛目標 1」的理由**：同上，且會讓 L5 恆與 L1 同級、排序時永遠打平。

**基礎建設按槓桿排，不按目標排。**

##### 兩種形式與 fallback（需求方 2026-08-14 裁定二）

**形式 A（解除阻塞）**：開卡時**指名被阻塞的線與卡**。急迫度**繼承自被阻塞方**
——被擋住的那張卡依 §3 判準 (a)–(d) 排在哪，這張 L5 卡就排在哪。

**形式 B（防止再發）**：開卡時**指名已實際發生、或已未遂、且有留痕可查的事故實例**。
急迫度由**再發率 × 代價**定，**不繼承**——它沒有被阻塞方可以繼承。

> **形式 B 是排序判準，不是准入閘門**（需求方 2026-08-14 裁定四）。它保證的是
> **論證被寫下來、寫下來之後可以被讀出來並駁倒**，**不保證擋掉每一張爛卡**。
> 下面兩道錨（不得假想、乘數以已實現的後果計）是這個排序判準的組成，**讀它們時
> 不要讀成閘門**——理由與爆炸半徑見〈形式 B 為什麼不做成准入閘門〉。

> ⚠️ **形式 B 不得用假想情境。**「這可能會出錯」不算；必須是**已經發生過或差一點發生**，
> 且**指得出留痕**（Issue 留言、Log、`incidents/` 錨點、逐字的失敗輸出）。
> 基礎建設幾乎都能宣稱自己防止出錯，**但沒幾個指得出已實現的事故**。拿掉這條，形式 B 會立刻
> 退化成上面那個「每張都能宣稱防止出錯、於是判準失去鑑別力」的老問題，只是換了個欄位名。

###### 乘數側的錨：兩個乘數也以「已實現的後果」計（需求方 2026-08-14 裁定三）

上面那條**只約束事故實例，不約束「再發率 × 代價」**——而急迫度是乘數算出來的。
只約束實例時，一張卡可以拿一件真實但無害的小事故過那道錨，再把代價寫成一條從未發生的災難鏈，
就取得高急迫度。**故兩個乘數同樣受錨**：

**再發率與代價，都必須以該事故實例【已實際造成的後果】計算。**
推測的下游災難鏈**可以寫**——它常常是這張卡真正的動機——但**必須明確標示為推測，且不計入排序**。

「已實現的後果」的界線：

- **包含**：該事故已經造成的、**實際存在過的壞狀態**
- **不包含**：該壞狀態若被觸發**會導致**的災難

**「已未遂」予以保留**，正是因為界線切在這裡：界線是**壞狀態已存在**，不是**災難已發生**。
未遂事故留下的壞狀態（守衛沒裝上、憑證是錯的、欄位是空的）本身就實際存在過，該計；
它「若被觸發會怎樣」不計。若把界線改切在「災難已發生」，已未遂會整類消失，
那等於要求先出事才准修——與 §0 目標 2「壞了會被發現」直接衝突。

**代價與再發率不得重複計分（需求方 2026-08-14 裁定四）**：
**代價以「單次事故實例」計；發生頻率一律歸再發率，不得寫進代價。**
「持續了 88 天」「每天都在發生」「三個月沒人讀」是**再發率**的材料——把它同時寫進代價
當嚴重度放大器，等於同一個事實在乘法的兩端各算一次，那張卡的分數會被平方級地灌大。
**這是算術錯誤，不是判斷歧異**，所以它可以直接修，而下面兩層人工判定不行（§6）。

**兩端驗證例**（一端排到最低、另一端排到高，判準才有鑑別力）：

- **`DEV-COMMIT-TRAILER-GUARD1`**（**假想卡，不是本專案的活卡**；勿與已結案的
  `DEV-TRAILER-GUARD-PR-CHECKOUT1`（#49）混淆）：加 hook 強制 commit trailer。
  事故實例**過得了**上面那道錨——「某次 commit 漏了 `Co-Authored-By`」`git log` 真的查得到、
  真的發生過。但**已實現的後果只有「一則 commit 少一行
  trailer」**：沒有任何裁決因此誤植、沒有任何碼因此錯誤合併。「歸屬不明 → 查核者判不出執行者
  身分 → 跨家族裁決誤植 → 錯的碼進生產」整條是推測，**不計分**。→ 代價趨近 0 → **急迫度最低**。
  **這正是本錨要排到最低的形狀**：三項全滿、分數很高、而這張卡近乎無價值。
  ⚠️ **「排到最低」不等於「擋下來」**——它照樣開得成卡，只是排在隊尾；擋不擋由 §2.0 的
  規劃 Gate 決定，不是由這條判準決定
- **`DEV-RESOURCE-VOCAB-ALIGN1`（#139）**：已實現的後果是 `#136` 的 Gate 證據無處可記、`#136`
  的規劃進度沒有狀態可表達、`#136` 自己的資源宣告從未被檢查過——三項都是實際存在的壞狀態，
  但都是「人撞到、人繞過」，沒有資料損害也沒有錯誤合併。**代價不大 × 再發率高 → 中等**
- **`OPS-BACKUP-EMPTY1`**：**單次代價**是「**那一次的 VPS 整庫備份 cron 產出 20-byte 空檔**，
  主站 `public` schema（18 張表）當日零可用備份」（`docs/research/OPS-BACKUP-EMPTY1_RESULTS.md`
  §1、§2.1）——實際存在過的壞狀態。**再發率**是「連續 88 天每天全中、且失敗無人接收」，頂格。
  「真的需要還原時還不回來」是未發生的災難，**不計分**。
  → 頂格再發率 × 一次就失去該日的整庫可還原性 → **急迫度高**。
  **這張卡是本判準必須排得高的形狀，收掉重複計分後依然排得高。**
  ⚠️ 本檔前一版把「連續 88 天」寫進**代價**當嚴重度放大器，**那是重複計分**（同一個事實在
  乘法兩端各算一次），已依裁定四更正；**更正後結論不變。**
  ⚠️ 曝險僅適用 VPS 那支整庫 cron——`cpbl` schema 同期有每日已驗證備份（同報告 §2.1），
  **不得讀成「88 天完全沒有備份」**

> ⚠️ **本錨補的是「乘數可以憑空寫」，不是把形式 B 機械化，更不是把它變成閘門。**
> 「已實際造成」的判定**本身仍然是人工的**——那是刻意的，逐項的下游承接見 §6。

**Fallback**：兩種形式都指不出來 → **急迫度最低，且不得認領**。

> 病灶是**判準對「預防型」的卡未定義**，不是把它排低。
> **一個回傳「無法判定」的判準比回傳「最低」更糟**——後者至少能排序。
> fallback 的作用就是讓判準恆有答案。

###### 形式 B 為什麼不做成准入閘門（需求方 2026-08-14 裁定四）

**形式 B 的作用是讓論證外顯、可被讀出來並駁倒，不是擋掉所有爛卡。**
本檔 iteration 2–4 連續三輪收緊這條判準，每一輪都找得到新的洞；第三輪找到的洞說明了原因：

**兩個失效方向由「已實現的後果」這同一個未定義詞控制。** 收緊到能把
`DEV-COMMIT-TRAILER-GUARD1` 那種形狀排到最低，同一個收緊會連
`OPS-SCHEDULE-FAILURE-BLIND1`（#132）那種**正當的偵測器類卡**一起殺掉。原因是
**偵測器的價值本質上大部分落在推測側**：#132 的事故實例是 `INC-2026-08-10-A`／`-B`
兩個排程同日失敗、三天無人知，**已實現的後果是「三天內沒有任何訊號，最後靠 PM 為了別的事
翻 `launchctl list` 才看到」**；而「下一次失敗照樣沒人知道，且那次可能更貴」整條是推測，
依錨**不計分**。**把錨收緊到足以把 trailer 卡壓到隊尾的力度，同一個力度會把 #132 一起壓下去**
——而 #132 正是 §0 目標 2 最直接的卡。**一個往哪邊調都會壞的旋鈕，代表這個旋鈕不該存在。**

**論證外顯已經在起作用，實證在本卡自己身上。** iteration 2–4 期間出現三次爛論證：
規劃者為 `#139` 寫的代價「兩張卡同時寫同一張生產表」從未發生；拿 `#136` 的序列碰撞
當「可逆性缺口」的佐證，而那份裁定實際判 `#136` **可逆**（§3）；把 `#130` 自己標成形式 A，
而它指不出被阻塞的卡（R5-001）。**三次全部是被人讀出來的，沒有一次是規則擋出來的。**
規則做的事只是逼人把理由寫下來——**寫下來之後爛論證藏不住**，而藏不住就夠了。

> ⚠️ **這份實證有取樣限制，結論要連觀測條件一起讀**（R6 finding `CPBL-ROADMAP1-R6-002`）：
> **上述三次全部發生在同一張卡上，而本卡跑了六輪跨家族查核；一般 L5 卡只有一輪。**
> 因此「論證外顯 → 會被讀出來」目前**只在高強度審視下被證實**，
> **常態一輪查核下未經驗證**。這**不推翻**本節的定位（形式 B 是排序判準而非准入閘門，
> 且寫下來仍嚴格優於不寫），但它是「藏不住就夠了」目前唯一的實證基礎——
> **不得把它讀成「任何審視強度下都藏不住」**。逐項處置見 §6。

**爆炸半徑有界，這是定位改寫的前提。** 一張爛 L5 卡混進來之後還要穿過四道：

- **§2.1 執行 WIP＝1**：L5 一次只跑一張，它佔掉的就是那一個位子，不會擴散
- **§2.0 規劃 Gate**：進 `📥Backlog` 前需求方核可
- **§2.2 三日時鐘**：逾三天必須先答三問（卡還成立嗎／前提變了嗎／驗收條件仍可執行嗎），
  **由需求方裁，執行者不得自行決定繼續**
- **§2.6**：判定為**錯誤開卡**即 `🛑已停止` ＋ **刪除實作**，報告是唯一倖存者

**最壞情況是浪費一個 L5 位子三天，然後被強制重新論證。**

**這個取捨與本檔自己的哲學一致，而前三輪的補錨不是。** §2.6 記著需求方的裁定原文
**「知道要修正的點就是該卡成果」**——本檔早就相信「**讓錯誤進來，但便宜地丟掉**」，
連被停止的卡都還留下一份報告當交付物（`#115` 即實例）。前三輪把形式 B 往「先擋住所有爛卡」
的方向收，**與同一份文件在 §2.6 已經寫下的立場相反**。這是本次改寫最強的內部依據。

> ⚠️ **四道攔截沒有一道是機械的**（§6）——WIP、規劃 Gate、三日時鐘、§2.6 的清理證明
> 全部由人執行。所以「爆炸半徑有界」的界是**紀律劃的，不是守衛劃的**。
> 這不推翻定位改寫（本來要換掉的就不是守衛，而是「假裝判準擋得住」），
> 但**不得把它讀成「有四層保險」**。`#115` 就是實測：它的假前提**沒有被前三道任何一道
> 在事前識破**，是執行者讀出來、再依 §2.6 停止的。**真正接住它的是最後一道
> ——事後、便宜地丟掉**，不是前三道。這正是定位改寫在賭的東西，也是它的風險所在。

##### 三張實卡的判準結果

實證是本檔 iteration 2–3 期間的三張 L5 卡（`#137` 已結案，`#139` 與 `#130` 論證見下）：

| L5 卡 | 形式 | 論證 |
|---|---|---|
| `DEV-ROADMAP-GATE-DERIVED1`（#137） | **A** | 被阻塞的是 **L5 的 #130**——本卡被 `roadmap_lines.py` 的 `GATE_OVERRIDES` 表擋住（R3-001），`v9` 已移除 |
| `DEV-RESOURCE-VOCAB-ALIGN1`（#139） | **B** | 2026-08-14 三項實撞，非假想 |
| `DOC-CPBL-ROADMAP1`（#130） | **B** | 形式 A 讀法在本卡身上不成立，改以已發生的開卡事故論證 |

**`#139` 的形式 B 留痕**（權威來源為該卡的 Issue 留言，本檔不複述細節）：`gate_evidence`
全庫 grep 零命中，致 `#136` 的 Design Gate 裁定**無處可執行**；`🧭規劃中` 被 `wfcli` 逐字拒收，
致 `#136` **沒有狀態可表達規劃進度**（同一份逐字輸出見 §2.0）；`db:<env>:cpbl` 被 `wfcli` 的
grammar 拒收、且環境名有四種寫法，致 **`#136` 自己的資源宣告從未被檢查過**。
急迫度不繼承而由再發率 × 代價定：**再發率**高（每一張新開的碰 DB 卡都會撞第三項），
**代價依「已實現的後果」計則不大**（三項都是「人撞到、人繞過」）→ **中等**。
⚠️ 該卡原先陳述的代價「兩張卡同時寫同一張生產表」**從未發生**，已依乘數側的錨在該卡上重寫；
**本檔不再引用那條推測鏈**。

**`#130`（本卡）為什麼不是形式 A**：它擋住的是「前四條線的取捨沒有唯一依據」，
而**取捨不是一張卡**——照形式 A 的「須指名卡」，它指不出來。**這正是 R5 finding
`CPBL-ROADMAP1-R5-001` 抓到的形狀**（當時文件拿 `#139` 當形式 A 的示例，而 `#139` 同樣指不出卡）。
本卡因此改走形式 B。**事故實例**（全部已發生、指得出留痕）：`#115` 依假前提開卡並執行至停止
（§2.6）；`#131`／`#132` 於 2026-08-13 未過規劃 Gate 即開卡派工；`#138`／`#139` 於 2026-08-14
同形再犯（§2.7）；`0948bae` 版據以排序的 `⏰ 2026-08-17` 時效被 `#131` 的 Discovery 推翻（§3）。

**已實現的後果**（依上述錨，逐項都是實際存在過的壞狀態，非推測）：一張前提為假的卡**實際被
執行到停止**，投入只剩一份報告（`#115`）；四張未過 Gate 的卡**實際落在 `📥Backlog`／被派工**，
需求方事後逐張人工退回 `💡需求`（`#139` 其後另因上游轉 `⏸阻塞`）；一份權威文件**實際以一個
假時效排序過前四條線**（`⏰ 2026-08-17`）。**代價不大**——全是「人開錯、人退回」的往返，
沒有資料損害、沒有錯誤合併、沒有對外數字出錯。**再發率高**：規則被重述的**同一天**再次被繞過，
同形已三次。→ **急迫度中等**（代價小 × 再發率高），與本卡的 T2 分級一致。

> **推測、依錨標示且不計入排序**：若繞過持續下去，終將有一張未經規劃的 T4 卡直接寫進生產。
> **此情境從未發生**，僅作為本卡的動機記錄。

> ⚠️ **這條規則沒有機械執行者**（§6），**形式 B 的兩層判定尤其是人工的**
> ——「不得假想」（事故實例）與「已實現的後果」（兩個乘數）都由 PM 讀，沒有機器分得出來。
> **這是刻意的**（裁定四）：形式 B 是排序判準，人工判定是它的**性質**而非待修的缺陷，
> 下游由 §2.0／§2.1／§2.2／§2.6 承接——逐項見 §6。
> 而且欄位與判準目前不對齊：§6 的維護表載明 `wfcli open` 已**機械強制**填「服務的原始目標」，
> 但依上述裁定 L5 的卡在該欄裡**填不出 §0 的目標**。過渡做法是在該欄填**形式 A 的被阻塞線與卡**
> 或**形式 B 的事故實例**，並由 PM 在 §2.7 的 checklist 上人工分流；
> **欄位語意的窮舉對齊屬 `DEV-RESOURCE-VOCAB-ALIGN1`（#139），不在本檔射程**。

---

## 2. 執行規則

### 2.0 規劃生命週期（基線 1）

**所有新卡一律由 `💡需求` 開始**，經 `🧭規劃中` 完成 **Discovery → Design → Plan** 三階段，
**才可進 `📥Backlog`／執行**。

```
💡需求 ──► 🧭規劃中 ──────────────────► 📥Backlog ──► 認領 ──► 執行
           Discovery → Design → Plan
           （階段不可跳過，產物深度依 tier）
```

- **階段不可跳過**，但**產物深度依 tier 調整**——T1 的 Design 可以是兩句話，T4 不行
- **T4／Initiative 必須進行逐題 `grilling`**（`AI_WORKFLOW.md` §3.1：同步對抗式質詢真對話，**brief 不得代替對話**）
- **全專案規劃 WIP ＝ 1**。規劃是需求方的頻寬，不是執行者的——同時開兩場質詢等於兩場都做不深

#### `🧭規劃中` 已經存在，但沒有人用——能力補上了，代價還在

⚠️ **本節在 2026-08-21 之前逐字寫著「`🧭規劃中` 不是『WF 尚未支援』，是它在機械面根本沒有」，
並附一段 `wfcli` 的錯誤訊息當證據。那句話已經是假的。** `ai-workflow#102`（`ae8f741`，
2026-08-18）補上了 `💡需求`／`🔬研究中`／`🧭規劃中` 三個授權前階段。兩處機械面各自可重跑：

```bash
# 1) 狀態面：Project #4「交付狀態」的選項
gh project field-list 4 --owner ruan6047 --format json | python3 -c \
  "import sys,json;print([o['name'] for f in json.load(sys.stdin)['fields'] if f['name']=='交付狀態' for o in f['options']])"
# 2026-08-21 實得：['💡需求','🔬研究中','🧭規劃中','📥Backlog','⏳待執行','🔨執行中','🚧進行中',
#                  '🔍待查核','✅通過','📦已合併','🏁完成','↩退回','⏸阻塞','🚨已升級','🛑已停止']

# 2) writer：wfcli 的 STAGE_STATUS（handoff_cmd.py:87-93）
git -C ~/Dev/ai-workflow show ae8f741:cli/src/wf_cli/commands/handoff_cmd.py | sed -n '87,93p'
# 2026-08-21 實得：STAGE_STATUS = {"requirement": "💡需求", "research": "🔬研究中",
#                  "planning": "🧭規劃中", "implementation": "🔨執行中", "review": "🔍待查核"}
```

> ⚠️ **本 repo 的 `.ai-workflow` submodule 指標仍釘在 `f207d2e`，那個 SHA 的 `STAGE_STATUS`
> 只有 `implementation`／`review`。** 能力在 `wfcli` 的**安裝來源**（`~/Dev/ai-workflow`，
> 其 `origin/main` 已含 `ae8f741`）上有，在本 repo 釘住的那一份上沒有——
> 兩者對不上時以實際被執行的那一份為準。submodule bump 屬另一張卡，不在本節射程。

**⛔ 代價沒有因此消失，只是原因換了**——從「**機械面做不到**」換成「**能力已存在而流程未採用**」：

- **現況分布實測**（2026-08-21，`gh project item-list 4 --owner ruan6047 --format json --limit 300`，
  全 **175** 個 item）：`🧭規劃中` **0 張**、`🔬研究中` **0 張**。而能力上線後這三天內，
  至少 `#161`（08-21 20:25 CST）與 `#162`（08-21 22:16 CST）兩張卡的 Log 逐字記著
  「規劃 Gate 通過」——**規劃確實在發生，只是沒有走那個狀態**（`#161` 現況 `📥Backlog`、
  `#162` 現況 `🔨執行中`）。⚠️ 本檔沒有 Project 欄位值的變更歷史，**不能斷言那兩張卡
  「從未」短暫停在 `🧭規劃中`**；能斷言的只有「現況分布為 0」與「兩張卡的規劃期不是
  以該狀態被記錄下來的」。
- **原本那段代價的原文照樣成立**：規劃 Gate 已過、Plan 未完成的卡**沒有狀態可表達**，
  在板上與從未動過的卡**完全同形**——`💡需求` 的卡在 §3 拿到的 Gate 欄**逐字相同**，
  無論它已完成 Discovery 與 Design 或從未被碰過。**§3 因此看不出規劃中的卡**。
  舊實例 `OPS-PROD-SYNC-SEQ-COLLISION1`（#136）已於 2026-08-14 結案轉 `🏁完成` 而離開
  §3 區塊，**但代價本身沒有隨它消失**——`#161`／`#162` 是同一形狀的新實例。
- **過渡做法（卡停在 `💡需求`，規劃進度只能寫進 Issue 留言）今日仍是實際做法**，
  但它**不再有機械面的藉口**。要改的是流程：規劃期實際走 `handoff --next-stage planning`，
  且不要用 `--status`（該旗標的說明逐字是「覆寫依 next-stage 推導出的交付狀態」）把它蓋掉。
  ⚠️ **本段原載的機械藉口已於 2026-08-21 消失**：`wfcli open` 的預設落點已由 `📥Backlog`
  改為 `💡需求`（`ai-workflow` `1531666283f2e1b55e92e103f2f13f8fd2fc131e`，
  `cli/src/wf_cli/card.py:295` 現值逐字 `delivery_status: str = "💡需求"`），⇒ **新卡落點
  本身已正確，不再須事後以 `handoff` 更正**。`open` 至今仍**沒有 `--status` 旗標**
  （`open_cmd.py` 對 `"--status"` 零命中），但那已不構成落點問題。
- **無法核對 Gate 證據時標 `not-asserted`**，**不得默認通過**；`not-asserted` 是永久紀錄，
  **WF 恢復後不得補寫成已通過**（基線 9）

> ⚠️ **`planning-started`／`cpbl-planning/v1`／`gate_evidence` 至今仍只是本檔裡的詞。**
> 2026-08-21 重跑 `git grep -n "gate_evidence\|planning-started\|cpbl-planning"`：三個 token
> **全庫只命中 `docs/ROADMAP.md` 自己**（`:281`、本註、§7 那一列）——**沒有 schema 檔、
> 沒有 writer、沒有儲存位置**。所以上面 `not-asserted` **標在哪裡目前仍然沒有答案**。
> ⚠️ 本註前一版寫「`gate_evidence` 只出現在 `docs/tasks/DEV-RESOURCE-VOCAB-ALIGN1.md`」，
> **該句同時已過期**：該檔今日不含此 token，而 `DEV-RESOURCE-VOCAB-ALIGN1`（#139）
> 已於 2026-08-18 CLOSED。**這個能力缺口沒有因為那張卡結案而被補上。**

### 2.1 執行 WIP（基線 2）

**每線 WIP ＝ 1**，五條線故全專案執行上限為 5。

| 佔實作 WIP | 不佔 |
|---|---|
| `⏳待執行`、`🔨執行中`（舊制 `🚧進行中`）、`↩退回`、`🔍待查核`、`🚨已升級` | `💡需求`、`🧭規劃中`（**選項已存在但零使用**，見 §2.0）、`📥Backlog`、`⏸阻塞`、`✅通過`、各終態 |

**`🔍待查核` 佔額度**——查核也是進行中，而且卡在查核比卡在實作更容易被忽略。

**`💡需求`／`🧭規劃中` 不佔實作 WIP**，所以**規劃階段的唯讀查證可以與其他線的執行並行**。
這不是漏洞，是設計：規劃要產出的正是「該不該排進去」的證據，那本來就得先做。

> ⚠️ **本規則上線當天即被違反**：L1 同時有 #53（`🔍待查核`）、#119（當時 `🚧進行中`）、#131（當時 `📥Backlog`）。
> 已依基線 6 處置——#119 轉 `⏸阻塞`、#131 退回 `💡需求`，處置後 L1 僅餘 #53 佔用。
> **問題不在規則，在狀態面與現實脫節時沒有分流**（R1-02）。
>
> ⚠️ **上段是 2026-08-14 的處置紀錄，不是現況**：其中「L1 僅餘 #53 佔用」已於
> 2026-08-15 結束——#53 當日改 `⏸阻塞`，依上表**不佔**實作 WIP。L1 的現值見 §3 區塊。

#### `✅通過` 不佔額度（需求方 2026-08-14 裁定）

`✅通過` **不是終態**（終態＝`🏁完成`／`🛑已停止`／`📦已合併`，見 `roadmap_lines.py` 的
`CLOSED_STATUSES`），所以「各終態」蓋不到它，本表原先漏了這一格。需求方裁定歸入**不佔**。

**理由**：`✅通過` 消耗的是**需求方的決策時間**，不是執行者的產能。讓它佔額度等於
**用人的決策延遲卡住 AI 的工位**——而需求方本來就是設計上的瓶頸，把兩者綁在一起是浪費艦隊。

**代價是積壓，而且積壓已經發生。** 查核通過到部署上線之間的卡不再佔任何額度，也就沒有任何
壓力把它推完；更糟的是**一旦轉入 `📦已合併`／`🏁完成` 就落進 `CLOSED_STATUSES`，
整張卡從 §3 排程表消失**——本檔於是看不見它。以本檔 as-of 快照實測「交付狀態
∈{`✅通過`,`📦已合併`,`🏁完成`} 且部署狀態 `⏸未部署`」者共 **7 張**（`#118`、`#78`、`#80`、
`#96`、`#52`、`#98`、`#81`），其中 **6 張已因終態離開 §3**，只有 `#118` 還看得到。

> ⚠️ **但這 7 張不能直接讀成「7 份未上線的碼」——部署狀態欄本身就是過期的。**
> `#78`（`OPS-WEB-DEPS1`，前端相依 1 critical ＋ 3 high，**皆為建置面**）與 `#80`
> （`UX-GAME-RECAP1`）的欄位都是 `⏸未部署`，但兩張卡自己的 Log 逐字記著部署已完成並驗證
> （`#78`：「deploy run 30785528406 成功…部署已驗證」；`#80`：「生產驗證通過」）。
> 真正明載「待部署」的是 `#96`／`#98`／`#81`（＋未 merge 的 `#118`）。
> **這一格是本節配套為什麼是清單而不是計數器的實證**：欄位會過期，而過期的計數器
> 比沒有計數器更危險——它讓人以為已經在看了。

**配套**：§2.7 的 `assign` checklist 增加一條積壓檢查，**答案要由讀卡得出，不是讀欄位**。
用清單不用新計數器的理由如上，且 §6 已自承這些規則全無機械執行者——再加一個沒人維護的常數
只是多一個會過期的數字。

### 2.2 時鐘與升級（基線 3）

**三日時鐘以「每個 iteration 的認領或退回」重新起算**——單一 iteration 逾三天才觸發重審。

> `0948bae` 版寫的是「從認領起算、不以最後事件重置」，**那會讓多輪迭代的紅線卡反覆誤觸發**
> （`ML-PITCHER-ER-REBUILD1` 跑了 8 輪、4 次查核，每一輪都在正常推進）。已依基線 3 更正。

**逾時的處置不是自動退回**，而是**在繼續之前必須先回答**：

1. 這張卡現在還成立嗎？（問題是否已被別的卡解掉、或已不再是問題）
2. 前提變了嗎？（基準退了多遠、上游是否已改、依據的文件是否仍為真）
3. 卡面驗收條件現在讀起來仍可執行嗎？

答完由**需求方裁**繼續／降級／關閉。**執行者不得自行決定繼續。**

#### 強制 `grilling` 重審的兩個觸發

- **兩次有效退回**，或
- **同一核心假設連續兩輪被證據推翻**

達成任一，**進第 3 輪前強制 `grilling` 重審**，**不得只寫「繼續」**。
重啟須建立**新的 escalation epoch** 並**保留舊證據**（不得覆寫）。

「有效」指**可計數的退回**——歸屬 coordinator／planner 的、外部阻塞造成的、同一 SHA 的重複 review 皆不計（`AI_WORKFLOW.md` §5）。

### 2.3 執行者必須停下回報的四種情形

**不得自行決定、不得繞過、不得「順手處理」**：

1. **卡面前提已不成立**——基準過期、上游已改、或問題已被別的卡解掉
2. **驗收條件自相矛盾、不可執行、或與 canonical 衝突**
3. **交付會含需求方才能下的判斷**——產品取捨、值不值得投資、關閉探索方向、待判讀案例清單
4. **發現射程外的問題**——回報，**不自行開卡，也不順手修**

實例（2026-08-08 單日）：PM 在派工包寫 T2 而板上是 T4；PM 下達「釘住母體凍結數字」直接違反 `statistical-redline.md` 第 9 條；#113 的執行者依據的 canon 寫「G4 凍結中」而權威來源早已解凍。**四次裡三次是執行者或查核者比 PM 先看到問題**——這條規則存在的理由就是讓他們有義務講出來。

### 2.4 開新卡必須引母卡目標原文

**深度有上限，方向沒有檢查。** `AI_WORKFLOW.md` §3.3 的鏈深上限（2 層）只是計數器，子卡可以在鏈深 1 的合法位置寫一個與母卡無關的目標。

- 子卡必須**逐字引用母卡的「服務的原始目標」**，並說明它如何服務**同一個**目標
- **引不出來就不是子卡**——脫鏈獨立開，並重新論證它服務 §0 的哪個目標
  （**L5 的獨立卡改走 §1 L5 的形式 A 或形式 B**；兩者皆指不出則急迫度最低且不得認領）
- 同時確認**母卡本身仍成立**

實例：2026-08-08 由 #113 的查核 finding `R1-02` 的 disposition 直接開出 #120，沒有任何一步問過這件事在整體規劃裡的位置。#120 與 #113 **是不同目標**——它其實該是脫鏈獨立卡。

### 2.5 過渡條款（舊卡怎麼辦）

**§1 的線別與 §2.4 的母卡目標，只對新卡強制。**

現有活卡都沒有這些欄位，而**補不進去**：`wfcli` 的動詞（`open`／`assign`／`deploy-declare`／`deploy-state`／`handoff`／`review`／`doctor`／`snapshot`）**沒有任何一個能修改已開卡的欄位**（`ai-workflow#12`）。而 `wfcli` 是狀態面唯一寫入通道，直接編輯 Issue body 即違規。

**處置：漸進補記。** 舊卡在 §2.2 逾時重新檢視觸發時，順便以 Issue comment 補記線別與母卡目標；躺在 `💡需求` 沒人動的卡先不補。**comment 不是欄位，`doctor` 看不到——這是已知的不完整，不假裝它等價。**

### 2.6 卡片不成立時的處置

§2.3 規定執行者發現前提不成立要**停下回報**。本節規定**回報之後怎麼辦**。

| 情況 | 狀態 | 實作（分支／worktree） | 報告的地位 |
|---|---|---|---|
| **錯誤開卡**——前提本來就假 | 🛑已停止 | **刪除** | **唯一倖存者** |
| **不可執行**——前提為真但做不到 | 🏁完成 | 保留並 merge | **交付物本身** |
| **前提已被別卡解掉** | 🛑已停止 | 保留 | 交付物 |
| **暫停**——等外部條件 | ⏸阻塞 | 保留 | — |
| **退回修正** | ↩退回 | 保留 | — |

**只有「錯誤開卡」刪實作。** 需求方裁定原文：**「知道要修正的點就是該卡成果」**。

#### 不可執行不是失敗

`ML-PITCHER-ER-REBUILD1`（#107）跑了 8 輪、4 次跨家族查核，結論是**自責分逐場重建不可行**，狀態是 🏁**完成**。交付物是一條否證鏈：兩個更忠於條文的修法實作後仍更差、且都沒修好自己的指標案例，全部撤回——**失敗的修正寫進報告而非默默撤回，因為兩者合起來才構成證據**。`INGEST-SCORELESS-INNING-PITCHER1`（#103）關閉 stats.cpbl 路線同理。

這類卡**不得標記為取消或停止**，也不得刪除實作。

#### `cancelled_with_report`：刪除需要的是證明，不是敘述（基線 10）

`0948bae` 版寫的是「PM 手動明示覆寫 `cleanup.py` 守衛」——**授權仍然是一段自由文字**，
誰都可以寫、寫錯不會被發現。WF `71df157` 已示範更安全的形狀：**proof kind 決定逐項授權，
留痕由實際執行結果產生，無法證明時保留資源**。

錯誤開卡的清理採同一形狀，證明類型為 **`cancelled_with_report`**，四項**缺一不可**：

1. **需求方的取消裁定**（具名、有時間）
2. **版本化報告已在 `main`**——報告寫進 `docs/tasks/<CARD_ID>.md`（該檔開卡時已在 main），接在原規格後加一節「取消報告」，且已 merge
3. **待刪分支的完整 tip SHA**（40 字元）
4. **刪除前 compare-and-swap 複驗**：重讀 tip，確認與第 3 項相同——**移動過就中止**

四項齊備才逐項授權 worktree → 本地分支 → **遠端分支**（需求方 2026-08-13 明示：**遠端一定要刪**，只刪本地等於沒刪）。

**報告不得只放 Issue 留言**——分支刪掉後它是唯一的東西，而留言 grep 不到、與規格分離。
報告至少要有：**前提為什麼不成立（附實查證據）／試過什麼、到哪一步失敗／歸屬（前提假記 planner）／真問題是否另開，且新卡前提須為實測**。

> ⚠️ **WF 未原生支援前，以上由人工執行。人工執行必須留下同等證據**（四項逐一寫進 handoff evidence），
> **不得宣稱已機械化**。`cleanup.py` 現行守衛在分支未 merge 時不授權任何刪除，依據是
> 「禁止靜默刪除工作內容」、裁定原文「噪音比不可逆的資料遺失便宜」——本節是**明示覆寫**，
> 而覆寫的正當性完全來自上述四項證明，不是來自 PM 的判斷。

實例：`OPS-DORMANT-SCHEDULE-AUDIT1`（#115）於 2026-08-13 停止，因其「排程交付後從未掛上」的前提為假（#53 載明未安裝是 Phase A 刻意邊界）。真問題改由 `OPS-SCHEDULE-FAILURE-BLIND1`（#132）承接，**前提為實測而非推測**。該卡從未派工、無分支，故無清理動作。

### 2.7 PM 祕書的過渡 checklist（基線 2）

`wfcli` 尚無 WIP／Gate／停損檢查，故**唯一 PM 祕書在 `open`／`assign` 前逐項確認**。
**這是紀律不是守衛**——它會失效，失效時的症狀是本檔 §3 與現實脫節。

`open` 前：

- [ ] 服務 §0 哪個目標？§0 三問答得出來？
      **L5 的卡改答兩種形式之一**（§1 L5）：
      **形式 A**——指得出**被阻塞的線與卡**嗎？（急迫度繼承自被阻塞方）
      **形式 B**——指得出**已發生／已未遂且有留痕**的事故實例嗎？**假想情境不算**（急迫度＝再發率 × 代價）
      **形式 B 追加一問**：**再發率與代價是否都以「已實現的後果」計？**
      寫出來的代價是**實際存在過的壞狀態**，還是「那個壞狀態若被觸發會導致的災難」？
      後者可以寫，但**必須標示為推測且不得計入排序**（§1 L5 乘數側的錨）
      **形式 B 再追加一問**：**代價是否只計「單次事故實例」？**
      「持續 N 天」「每天都在發生」屬**再發率**，寫進代價即**重複計分**（同上節）
      兩者皆指不出 → **急迫度最低且不得認領**，不是「無法判定」
      ⚠️ 以上各問**是排序判準不是准入閘門**：答得爛的卡照樣開得成，只是排在隊尾且論證
      留在卡面可被駁倒。**PM 的工作是逼它寫出來並讀它，不是在這裡擋下它**（§1 L5 裁定四）
- [ ] 屬哪條線？（`roadmap_lines.py` 判得出來，或已加進明列表）
- [ ] 若為子卡：母卡「服務的原始目標」原文引得出來？母卡本身仍成立？（§2.4）
- [ ] tier 是否落在紅線範圍（安全／金流／統計 ML／資料正確性／資安部署／production migration → T4）
- [ ] T4 卡面是否附具體化的紅線區塊？
- [ ] 碰 DB 的卡：spec 是否含 `db_scope`／`db_namespace`／`db_resources`／`migration_phase`？
- [ ] L4 卡：停損條件填了嗎？（§1 L4，補不出來不得認領）

`assign` 前：

- [ ] 該線 WIP 是否已滿？（§2.1）
- [ ] 該線有沒有 `✅通過` 未 merge、或「已 merge 未部署」（`📦已合併`／`🏁完成` ＋ `⏸未部署`）的積壓？（§2.1）
- [ ] 規劃 Gate 過了嗎？未過不得認領（§2.0）
- [ ] 資源宣告是否與其他活卡交集？

> ⚠️ **積壓那一條沒有機械守衛，且它問的欄位會過期**（§2.1 實測 `#78`／`#80` 欄位仍寫
> `⏸未部署` 而 Log 已記部署驗證完成）。**要讀卡的 Log，不是只讀部署狀態欄**；
> 讀到的是「這條線有沒有東西卡在通過與上線之間」，不是一個可信的數字。

> ⚠️ **2026-08-13 實績**：#131 與 #132 都在**未過規劃 Gate** 的情況下由 PM 直接開卡並派工，
> 兩張皆已依基線 6 退回 `💡需求`。**checklist 存在不代表被執行**——這一格是 §6 自承的同一個問題。
>
> ⚠️ **2026-08-14 又一次，形狀完全相同**：`DATA-OFFICIAL-STATUS-TIEBREAK1`（#138）與
> `DEV-RESOURCE-VOCAB-ALIGN1`（#139）於 15:05 開卡即落 `📥Backlog`，兩張的 Log **只有一行
> `open`**，沒有任何規劃事件。依 §2.0「未過 Gate 不得進 `📥Backlog`」，**那個落點就是違規**。
> 兩張都是本檔 iteration 2 送審當天開的——**規則被重述的同一天再次被繞過。**
> 處置屬需求方，本卡只記錄，依 §2.3 第 4 條不自行開卡、不代改狀態；
> **需求方已於同日將兩張退回 `💡需求`**，故 §3 的重生區塊都不再讀到 `📥Backlog`——
> `#138` 仍是 `💡需求`，**`#139` 其後另轉 `⏸阻塞`**（阻塞於 `ai-workflow#87`，見該卡 handoff 事由）。
> **違規發生過這件事不因退回而消失**——它是 §1 L5 認定本卡走形式 B 的留痕之一。

---

## 3. 現行排程

> **as-of `2026-08-22T01:59:00+08:00`（區塊所憑快照的取樣時刻）。本表是快照，不是即時視圖。**
> 狀態的事實來源永遠是 Issue／Project。
>
> **下面的區塊由指令產生，非人工列舉**，且**直接產自隨卡存檔的那份快照**
> [`research/DOC-ROADMAP-STALE-SYNC1_snapshot.json`](research/DOC-ROADMAP-STALE-SYNC1_snapshot.json)
> ——`gh project item-list` 的**逐字輸出、未經加工**，取樣時刻即上面的 as-of。
> （`DEV-ROADMAP-GATE-RESEARCH-STATUS1` 遞增 `SCHEMA_VERSION` 至 `v11` 後依**同一份快照**
> 重生；相對於 `v10` 的區塊，**只有版本註解那一行改變**，卡片列逐位元組相同——`v11` 補的是
> `🔬研究中` 的 Gate 導出，而這份快照裡 0 張卡帶該值。`v10`（`DEV-ROADMAP-LINES-SILENT-ZERO1`
> 的來源辨識）同理。⚠️ **「卡片列不變」是離線重現的必要不變量，不是不遞增版本的理由**。）
> **為什麼用這一份**：repo 內只有這一份存檔的看板快照，且驗證器的兩條路徑不等價
> ——`items` 路徑對「活卡缺卡ID」是 fail closed 的，`wfcli snapshot` 的 `cards` 在上游
> 就丟掉沒有卡ID的 item，故同樣通過時 `items` 路徑的宣稱**強一階**（見腳本 docstring）。
> **用它可離線重現本區塊的 `exit 0`**，不需連 GitHub：
>
> ```bash
> uv run python scripts/roadmap_lines.py --check docs/ROADMAP.md \
>   < docs/research/DOC-ROADMAP-STALE-SYNC1_snapshot.json
> ```
>
> 對帳**當前**看板（取即時值，不保證與上式同結果——原因見下）：
>
> ```bash
> gh project item-list 4 --owner ruan6047 --format json --limit 300 \
>   | uv run python scripts/roadmap_lines.py --check docs/ROADMAP.md
> ```
>
> ⚠️ **本區塊含 `#162` 自己那一列，故它對即時看板必然會漂。** 區塊在該卡 `🔨執行中`
> 時產生；`handoff` 送審後看板變 `🔍待查核`，即時對帳就會在**這一列**報 drift。
> 那不是區塊算錯，是「快照 vs 即時視圖」的定義差別（本節開頭的 as-of 聲明即為此）。
> **要判本區塊產生得對不對，用上面釘住快照的那一式。**
>
> 該腳本已隨 `DEV-ROADMAP-VERIFIER1`（#133）merge 進 `main`（`9e4b4ea`）。**`v1` 版的本檔
> 曾附一行指向不存在的腳本並宣稱清單可重現**——那是 R1 finding `CPBL-ROADMAP1-R1-03`。
>
> **區塊以 `<!-- roadmap-lines:begin/end -->` 界定**，沿用 Issue body `resource-claims` 的
> 既有慣例。**不靠 markdown 結構定位**——`v1`–`v4` 那樣做，連續三輪查核各找到一個新的
> 邊界情形（圍籬內的假 `## 3.`、重複標題、縮排／引言列、圍籬長度），清單沒有盡頭。
>
> `--check` 的三層：**版本**（區塊自陳版本須與腳本相同）、**集合**（雙向差集為空、無重複）、
> **逐欄**（`卡`／`#`／`tier`／`狀態`／`Gate` 逐欄比對）。`去留` 欄由需求方手填，**放行不比對**。
> 未歸屬、marker 不成對、區塊內縮排／引言列、欄數不符、必填欄位缺失、未知交付狀態
> ——一律 fail closed。
>
> **「線別」是藍圖分派，不是 Project 欄位**；兩者可不同步（見 §2.5）。
>
> **「去留」欄一律留白**——繼續／降級／關閉含產品取捨與值不值得投資的判斷，依 §2.3 第 3 條屬需求方裁定。
> **「下一個必要 Gate／阻塞條件」欄純由交付狀態導出，沒有逐卡覆寫**（`v9`／`DEV-ROADMAP-GATE-DERIVED1` #137 移除）。
> 狀態導不出來的逐卡事實（阻塞對象、閘門理由、在等哪一次部署）**不在區塊內**，見下節。

### 同目標內的處理順序（基線 5）

跨目標依 §0 排序；**同一目標內**依序：

1. **(a) 已在 production 對外產生錯誤**
2. **(b) 必要前置**——擋住其他卡的
3. **(c) 已有事故但缺防線**
4. **(d) 無已知受害的預防強化**

**同級由需求方於 Design Gate 明示排序。**

> ⚠️ **L5 的卡排不進上面這個框架。** 「跨目標依 §0 排序」預設每張卡都掛在某個目標上，
> 而需求方 2026-08-14 裁定 **L5 不服務 §0 的單一目標**（§1 L5），故 §0 排序對 L5 **無定義**。
> 依 §1 L5 的兩種形式分流：
>
> - **形式 A 繼承**：急迫度繼承自它解除阻塞的那條線與那張卡，被解除的對象越急該卡越急；
>   判準 (a)–(d) 套在**被阻塞的那張卡**上，不套在 L5 卡自己身上。實例是 #137 與本卡的關係
>   ——#137 排在 #130 之前不是因為它服務更高的目標，而是因為 #130 被它擋住，
>   即 (b)「必要前置」**由被阻塞方繼承過來**
> - **形式 B 不繼承**：沒有被阻塞方可繼承，改由**再發率 × 代價**定序。(a)–(d) 是**同一目標內**
>   的決勝，而 L5 不掛單一目標，故它對形式 B 同樣無定義。⚠️ **事故實例不得假想，兩個乘數
>   都必須以「已實現的後果」計，且代價只計單次實例、發生頻率一律歸再發率**（§1 L5）
>   ——**只約束實例不約束乘數時，乘數照樣可以憑空寫，這條排序等於沒有**：真實但無害的小事故
>   加一條從未發生的災難鏈，就能換到高急迫度。
>   ⚠️ **這三條是排序判準的組成，不是准入閘門**——它們讓論證外顯可駁，**不保證擋掉爛卡**
>   （§1 L5〈形式 B 為什麼不做成准入閘門〉）
> - **兩者皆指不出**：**急迫度最低且不得認領**（fallback），**不是「無法判定」**

> ⚠️ **本檔 `0948bae` 版在此舉了一個例子，而那個例子已被推翻。** 原文稱
> `DATA-BOX-DEEP-SILENT-FAIL1`（#131）的 7 場「在 2026-08-17 後掉出重抓窗」屬
> 「不做就永遠做不了」。#131 的規劃階段 Discovery 證明**那是假的**：`days_back` 是
> `cpbl-refresh-box-deep` 的位置參數（`run_refresh_box_deep.py:47`，預設 30），
> **不是物理限制**；放大到 60 在任何一天都涵蓋得到。該母體另漏算了 `kind=A` 的 9 場。
>
> **舉例被推翻之後，缺口本身是否仍成立？** 判準 (a)–(d) 確實只看嚴重度、不含
> 可逆性／時效維度——但本檔目前**舉不出第二個真正不可逆的例子**。因此本節記錄為
> **待驗證的觀察，而非已證成的缺口**：需求方若在某張卡上遇到真正的不可逆情形，
> 於 Design Gate 明示排序即可；**不因一個已被推翻的例子而預先加一條判準。**

#### 待 R4 重判：2026-08-14 出現的兩份新材料

**上面那段「降級為待驗證的觀察」已由 R3 查核者裁定恰當**，理由逐字是「因無第二個不可逆例子」。
以下兩份材料出現在該裁定之後，**列為候選證據送 R4 重判；本卡不自行把該段升級回「已證成的缺口」**
——推翻一個已生效的查核裁定不是執行者的權限。**兩份材料本身也都不乾淨，一併記下反面理由。**

**材料一：`#136` 排在 `#134` 之前，而需求方陳述的理由是可逆性。**
`OPS-PROD-SYNC-SEQ-COLLISION1`（#136）與 `DATA-PA-REBUILD-GAP1`（#134）同日在途，
需求方據以排序的是「#134 的損害是靜態的（7 場維持錯著，不擴大）；#136 的觀測軌跡缺口
每過一天就少一天且無法回填」。

> ⚠️ **本卡對這份材料的反面意見（請 R4 一併裁）**，兩點：
>
> 1. **「兩張同為 (a) 級」不成立。** #134 是對外服務**錯的**數字（7 場 PA 衍生表，消費者含賽況、
>    recap、WP／WPA、ΔRE24）＝(a)；#136 是生產資料自 2026-08-13T02:36Z 起**停止更新**，
>    而失敗交易整體回滾、prod 內容完好（#136 Design Gate §8 裁定原文）。**過時不等於錯誤**：
>    依 §0 前者屬目標 1、後者屬目標 2，本檔的線別歸屬也把它們分到 L1 與 L2。
>    若照 §3 現有規則「跨目標依 §0 排序」，**#134 反而應該排在 #136 之前**——實際排序與判準相反，
>    這讓材料一比需求方陳述的更強（不只是同級無法決勝，是判準被推翻）。
> 2. **但 (b) 必要前置很可能已經解釋了這個排序。** `scripts/refresh-cpbl-prod.sh` 的
>    `sync_revision_table game_source_revisions`（**`:230`** @ `de34c7e`）失敗後，其後 **`:234`**
>    起的**所有** `sync_table`（含 gamelog、pitch_tracking、PA build）都不會執行。
>    **#136 未修時，任何資料修正都到不了生產，包含 #134 自己的修法**。那正是判準 (b)
>    「擋住其他卡的」。若 (b) 已導得出同一個順序，**判準就不缺這個維度，缺的只是有人去套判準**。
>
>    > ⚠️ **行號釘 `de34c7e`，因為它已經漂了。** `#136` 的修法已於 2026-08-14 merge 進 `main`
>    > （`276114b`），同兩行現在是 `:349`／`:353`。**本點陳述的是修法 merge 前的狀態**
>    > ——材料的效力不變（它解釋的是當時為什麼那樣排），但**不得再讀成現況**。
>    > `#136` 已於 2026-08-14 結案轉 `🏁完成`，**故不再出現在 §3 的區塊裡**（終態卡離開排程表）；
>    > 本檔前一版在此寫著它「仍是 `🚧進行中`」，已隨本次重生更正。
>    > **本點自始至終陳述的是修法 merge 前的狀態**，該卡結案不改變材料一的效力。

**材料二：`#136` §8 的緊急通道裁定把低級事故定義為「不可逆的留痕／碼損害」**，並明確排除
「對外服務降級」，依據是 canonical §5 自己舉的三個例子。**該裁定確實把可逆性當成排序軸的判準**。

> ⚠️ **反面意見**：那是**緊急通道的二元閘門**（走／不走），不是 (a)–(d) 這種同級決勝；
> 而且它對 #136 的**實際裁定是「不算低級事故」**——即判 #136 **可逆**。
> 用一份判「#136 可逆」的裁定去佐證「#136 是不可逆的例子」，**兩者不能同時當真**。
> 該三句話的正確歸位是 §5（已依 #136 finding 5 納入），不必然是本節。

**本卡的整體讀法**：材料一的殘餘（`zone_before > 0` 的差分證據窗會被系統自身的振盪抹掉、
只能等下一個差分日）是 (a)–(d) 真的沒有的東西，**但它是「證據何時採得到」而不是「卡怎麼排」**。
故本卡**傾向維持 R3 的降級裁定**，但把材料完整交出，**由 R4 定奪**。

### L1 的裁定（基線 6，2026-08-14）

> **本節是 2026-08-14 的裁定紀錄，不是現況視圖。** 各卡現值一律以 Issue／Project 為準
> （活卡快照見 §3 區塊）。下列各條保留當時的裁定與理由；**其後已知失效的逐條就地標記**——
> 保留基線的決策脈絡不等於可以用現況式語氣宣稱已經不成立的事。

- **`#53` 是 L1 閘門**，Phase B 完成前**佔用該線 WIP**。
  **不得描述為「整卡已完成但看板失真」**——Phase B 確實未完成
  ⚠️ **其中「佔用該線 WIP」自 2026-08-15 起不再成立**：#53 當日由 `🔍待查核` 改
  `⏸阻塞`（需求方裁定，留痕見 #53 Issue Log `2026-08-15T00:29:39+08:00`），而依
  §2.1 的表 `⏸阻塞` **不佔**實作 WIP。**閘門身分與「Phase B 未完成」不受影響**——
  改判動的是阻塞的性質怎麼記在狀態面，不是 Phase B 完成了。理由見下節。
- **`#119` 轉 `⏸阻塞`並保留 worktree**，等 #53 結案；解阻後**重新驗證前提並啟動新 iteration**
- **`#131`／`#132` 退回 `💡需求`**，iteration 不增；#131 走 T4 `grilling`、#132 走 T2 規劃
- **#53 之後的 L1 順序**：`#119` → `D/97`（`INC-2026-08-13-A`，先進需求／規劃，**不直接開工**）→ `#131`

> ⚠️ **iteration 1 查到的新事實**：`#53` 的四項放行條件要求「計數須由腳本自 `refresh_log` 與
> `games` 產生為 artifact，不得人工聲明」，而**該腳本不存在**——`g4_phase_a_metrics.py`
> 只有 `equipped`／`requests`／`rollback` 三個子命令。**四項放行條件目前一項都判不了**，
> 而 Phase A 的碼已在生產運行。處置未定，屬 #53 自身射程。
> 另：#53 第 2 輪的 `APPROVE`（2026-08-05，寫在 Issue 留言）**從未經 `wfcli` 寫入狀態面**，
> 板上因此**一路停在 `🔍待查核`，直到 2026-08-15 需求方改判 `⏸阻塞`**（見下節）。

### 狀態導不出來的逐卡事實（原 Gate 欄覆寫）

`v9` 之前，下列註記硬編在 `roadmap_lines.py` 的 `GATE_OVERRIDES` 裡。**那個欄位是混血的**
——一半由狀態機械導出、一半是人寫的說明文字，而人寫的那半沒有真實性來源也沒有到期機制。
`DEV-ROADMAP-GATE-DERIVED1`（#137）把整張表移除，註記歸位到這裡：**本檔是本卡的宣告資源，
與必須保持一致的敘述同一個擁有者**。

- **`#53`／`#119`**：見上一節（L1 閘門與阻塞對象）——該節兩處失效**已就地標記**。
  **2026-08-15 改判 `⏸阻塞` 的理由**：狀態詞描述錯了阻塞的性質——真正的阻塞是
  Phase A → Phase B 的四項放行條件**沒有量測工具**，不是「等查核者進場」
  （留痕：#53 Issue Log `2026-08-15T00:29:39+08:00`）。上一節的其餘敘述——四項放行
  條件判不了、Phase A 的碼已在生產——**未被該裁定推翻**。
  `#119` 不變：`⏸阻塞`、保留 worktree、解阻對象仍是 #53。
- **`#131`**：原註記要求「規劃階段先做唯讀查證（31 場是否真的都未進快照／7 這個數字重算）」
  並附一條 `⏰ 2026-08-17` 時效。**兩者都已作廢**——查證已由 #131 的 Discovery 完成，
  而時效所依據的前提已被同一份 Discovery 推翻（見上上節）。**這條就是移除逐卡覆寫的實證**：
  它寫於 2026-08-14 上午、同日下午即過期，而消費它的正文已經改對，區塊卻改不掉。
  ⚠️ **本卡已於 2026-08-19 CLOSED（`🏁完成`）**，終態故已離開 §3 區塊；本條自此只是歷史留痕。
- **`#109`**：等需求方完成兩項手動部署（生產端手動跑 migration 071／072、
  `com.cpbl.weekly-box-revisions` launchd bootstrap）。
  ⚠️ **launchd 那一項早已完成**：2026-08-21 重跑 `launchctl list | grep weekly-box-revisions`
  得三欄 `-`／`0`／`com.cpbl.weekly-box-revisions`（已載入；上次結束碼由 2026-08-14 R1 查核者
  觀察到的 `1` 轉為 `0`）。
  ⛔ **生產端 migration 071／072 是否已套用，本檔未查證**——查證須連生產，屬需求方動作。
- **`#79`**：⛔ **它不是在等驗證後結案，它是被裁定為永遠不會結案的假活卡。**
  需求方 2026-08-18 裁定**不補走部署狀態**
  （[裁定留言](https://github.com/ruan6047/cpbl-analytics/issues/79#issuecomment-5327710893)）：
  碼已隨某一次主站 submodule bump 進生產（`f9f2399` 是主站現行指標 `d5e60c5` 的祖先，
  `git merge-base --is-ancestor` 為真），但板上部署狀態仍是 `⏸未部署`，而
  `handoff --next-stage release` 的閘門要求需部署卡在 `✅已驗證` 前不得 release。
  **走完中間那幾步等於在留痕裡造沒發生過的事件序，需求方寧可留下可見的不一致。**
  後果：本卡持續以 `📦已合併` ＋ Issue OPEN 停在看板上（canonical §4.4 逐字的「假活卡」），
  且因 `📦已合併` ∈ `CLOSED_STATUSES` 而**不在 §3 區塊內**——看板上有、排程表上沒有。
  ⚠️ 該裁定的射程僅為「碼在不在生產」，**沒有驗證該功能在生產上是否正常運作**。

> ⚠️ **搬家不會讓它們自己更新。** 移除逐卡覆寫解決的是「文件擁有者修不掉它」，
> **不是「它不會過期」**。實證就在上面這份清單裡：`#109` 的兩項手動步驟中，
> launchd bootstrap **至遲於 2026-08-14 已完成**——R1 查核者實跑 `launchctl list`
> 觀察到 `com.cpbl.weekly-box-revisions` 的 `LastExitStatus=1`（載入了、跑了、失敗了）。
> **搬過來的第一批註記裡就有一條已部分過期。**
> 這些註記的事實來源永遠是各自的 Issue；本節是快照，與 §3 開頭的 as-of 聲明同一效力。

<!-- roadmap-lines:begin -->

<!-- cpbl-roadmap-lines/v11；活卡 37；每線 {'L1': 8, 'L2': 7, 'L3': 6, 'L4': 11, 'L5': 5} -->

### L1 資料正確性（8 張）

| 卡 | # | tier | 狀態 | 下一個必要 Gate／阻塞條件 | 去留 |
|---|---|---|---|---|---|
| `DATA-BOX-REVISION-SNAPSHOT1` | #109 | T2 | ⏸阻塞 | 解除阻塞條件（阻塞對象與解阻後的處置見該卡 Issue 的 handoff 事由） | |
| `DATA-RE24-PROD-REBUILD1` | #119 | T4 | ⏸阻塞 | 解除阻塞條件（阻塞對象與解阻後的處置見該卡 Issue 的 handoff 事由） | |
| `DEV-VERIFY-TM-ASSERTS1` | #50 | T2 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `INGEST-GAME-TM-REFACTOR1-G4` | #53 | T4 | ⏸阻塞 | 解除阻塞條件（阻塞對象與解阻後的處置見該卡 Issue 的 handoff 事由） | |
| `INGEST-LIVE-RECONCILE1` | #54 | T4 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `INGEST-POSTGAME-FINALIZE1` | #57 | T3 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `INIT-OFFICIAL-DATA1` | #61 | T4 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `MATCHUP-DATA2` | #63 | T4 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |

### L2 每日鏈可靠性（7 張）

| 卡 | # | tier | 狀態 | 下一個必要 Gate／阻塞條件 | 去留 |
|---|---|---|---|---|---|
| `API-INFO-UNRESOLVED-GAMES1` | #127 | T3 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `OPS-BACKUP-DR1` | #71 | T3 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `OPS-POSTGAME-OBSERVE1` | #73 | T2 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `OPS-REMOTE-CUTOVER1` | #74 | T4 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `OPS-REMOTE-PROBE1` | #75 | T3 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `OPS-REMOTE-ROUTE1` | #76 | T3 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `OPS-REMOTE-WORKER1` | #77 | T4 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |

### L3 產品／UX（6 張）

| 卡 | # | tier | 狀態 | 下一個必要 Gate／阻塞條件 | 去留 |
|---|---|---|---|---|---|
| `INIT-GAME-RECAP` | #60 | T4 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `INIT-PRODUCT-UX` | #62 | T3 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `UX-COMPLETED-JUDGMENT-DATE-BOUNDARY1` | #161 | T4 | 📥Backlog | 認領（線 WIP 須有空位） | |
| `UX-GAME-META-COMPLETED1` | #148 | T2 | 📥Backlog | 認領（線 WIP 須有空位） | |
| `UX-TEAM-FIELD-HIST1` | #82 | T3 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `UX-WINPROB-CURVE-MIGRATE1` | #97 | T3 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |

### L4 ML／研究（11 張）

| 卡 | # | tier | 狀態 | 下一個必要 Gate／阻塞條件 | 去留 |
|---|---|---|---|---|---|
| `ML-FIELD-LINEUP1` | #64 | T4 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `ML-FIELD-OAA-VAL1` | #65 | T4 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `ML-FIELD-OF1` | #66 | T4 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `ML-PA-SIM-CONTEXT1` | #67 | T4 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `ML-PA-SIM-TEAM1` | #68 | T4 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `ML-PT3` | #69 | T4 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `ML-SIM2` | #70 | T4 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `ML-WP-ASOF-PUSHDOWN1` | #102 | T2 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `ML-WP-CAL1-RERUN1` | #104 | T3 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `ML-WP-ROLLWIN1` | #95 | T4 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `RESEARCH-REASON-RESTATE1` | #105 | T2 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |

### L5 開發／文件基礎（5 張）

| 卡 | # | tier | 狀態 | 下一個必要 Gate／阻塞條件 | 去留 |
|---|---|---|---|---|---|
| `DEV-CI-LOCALE-UNDECLARED1` | #129 | T1 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `DEV-ROADMAP-LINES-SILENT-ZERO1` | #143 | T2 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `DEV-WP-DISCLOSURE-SOURCE1` | #147 | T3 | 📥Backlog | 認領（線 WIP 須有空位） | |
| `DOC-LIVELOG-SEMANTICS-GAP1` | #108 | T1 | 💡需求 | 規劃 Gate：Discovery → Design → Plan，需求方核可後才進 Backlog | |
| `DOC-ROADMAP-STALE-SYNC1` | #162 | T2 | 🔨執行中 | 交付並 handoff 送審 | |

<!-- roadmap-lines:end -->

### 已開卡的衍生事故（原「尚未開卡的已知問題」）

`INC-2026-08-13-A`（`D/97` 續賽後 PA 衍生表未重建）**已於 2026-08-14 開卡**：
`DATA-PA-REBUILD-GAP1`（#134，L1、T4）。

⚠️ **開卡當時的三項描述已由該卡的 Discovery 推翻**，本檔一併更正：

| 本檔曾寫 | 事實 |
|---|---|
| 受害面**單場**（`D/97`） | **7 場**（`A/209`、`A/215`、`A/235`、`D/97`、`D/119`、`D/180`、`D/184`） |
| 成因是「PA build 目標挑選以有無 PA 列判斷」 | 判準是 `game_recap_builds.state='published'`；**真正觸發是 2026-08-10 的 run 在 `_pa_build_step` 之前崩掉**＋`[昨天,今天]` 的窗不補課 |
| 「續賽場次再發生時會重現」 | 7 場中 **5 場不是續賽**。缺陷類別是「**窗後任何 livelog 變動**」 |

需求方 2026-08-14 另裁定**推翻 `DATA-INCOMPLETE-BOX-INGEST1`（#113）的「不處置」**，改為重建——該裁決的前提「續賽後排程會自己重建」已被上述證據推翻。

**這一則的教訓屬本檔 §0 的範圍**：`D/97` 的損壞在 DB 裡活了四天無人知，而 `D/119` 更早（08-08）且至今未被任何機制標示——**目標 1 那條線的「可重跑對帳路徑」在 PA 這一層並不存在。**

---

## 4. 驗收政策

沿用 `ai-workflow/docs/ROADMAP.md` **§4「驗收政策」@ `52839f0`**（大項目通過即驗收、細節轉 Backlog；判準是後果而非嚴重度標籤）。**本檔不複述其判準表。**

CPBL 的加嚴：**目標 1／2 的卡不適用「細節轉 Backlog」**——對外數字錯誤與靜默失效正是 §4 所指的「會造成低級事故」，一律退回。

**目標 1／2 的修復另加一條（基線 7）**：除了證明本次修好，**還要以事故回放或 fault injection 證明同型失敗會被擋下**。只證明「這一筆修好了」不算完成。

## 5. finding 的處置

沿用 `ai-workflow/docs/ROADMAP.md` **§5「finding 的處置」@ `52839f0`**（先看整體規劃、不是看一個修一個；三項禁令）。**本檔不複述。**

CPBL 的補充：**收到 finding 後開卡前，先執行 §2.4 的母卡目標檢查。** 引不出母卡目標的 finding，要嘛脫鏈獨立論證，要嘛只記錄不開卡。

### 緊急通道的三條界線

**出處不是本檔。** 這三句是 `OPS-PROD-SYNC-SEQ-COLLISION1`（#136）Design Gate 裁定的
§9 finding 5，該卡在自己身上把 canonical §5 的「緊急修，**不排隊**」逐條套過一次之後
交給本檔，需求方 2026-08-14 裁定納入。**是 #136 的產出，不是本檔自己的發現。**

- 「緊急修，不排隊」授權**插隊**，不授權**跳閘門**——兩者是不同的軸
- 低級事故的判準是**不可逆的留痕／碼損害**（照 canonical §5 自己舉的三個例子），
  不是「對外服務降級」
- 緊急通道通往的是 **mitigation 不是 fix**：mitigation 必須可逆、不得消耗結案證據、
  **且不得被記為修好**

第一條處理的是「排序軸 vs 閘門軸」：§5（本節）管排序，§2.0 管閘門。
**#136 自己就是兩者同時成立的實例**——當天開卡、當天質詢、插了 25 張卡的隊（排序軸放行），
同時走完 T4 完整質詢（閘門軸未放行）。**缺口不在 §2.0，在「不排隊」被讀成「不規劃」。**

第三條的反例也來自 #136 同一天：走快速通道那半天產出的是錯誤診斷、一次花掉的授權、
無效的 prod 寫入，以及一個「已解決」的假信念——**當天唯一擦到「留痕重建不出來」的動作，
是快速通道自己做出來的。**

---

## 6. 本檔的維護

### ⚠️ 先承認：本節不符合 §0 目標 2 的判準

§0 目標 2 寫著「**靠人記得看不算**」。而本節底下的每一條，執行者都是人。**這份文件在管別人時要求機械執行者，管自己時只有紀律。**

**同樣的自承適用於 §2.0 的規劃 Gate、§2.1 的 WIP 上限、§2.2 的三日時鐘、§2.4 的母卡目標、§2.7 的 checklist**——它們全部沒有機械執行者。唯一有**守衛腳本**的是 §3 的歸屬對帳（`roadmap_lines.py`，fail closed）與 §2.6 的清理證明（四項缺一不可，但由人工核對）。⚠️ **有腳本不等於有執行者**：`roadmap_lines.py` 自己確實 fail-closed，但**沒有任何東西會去跑它**——2026-08-14 至 08-21 的七天內觸發點發生 11 次、實跑 0 次（實測見下節）。**這一條同樣屬於「管別人時要求機械執行者，管自己時只有紀律」。**

**需求方 2026-08-14 裁定新增的三條規則同樣沒有機械執行者**，比照上列誠實標注：

| 新規則 | 沒有守衛的具體形狀 |
|---|---|
| §1 L5「開卡須走形式 A／形式 B，兩者皆指不出則最低且不得認領」 | `wfcli open` 強制填的是「服務的原始目標」欄，**兩種形式都沒有對應欄位**，也不檢查形式 A 指名的線與卡是否存在。填錯或留白只有 PM 讀得到（欄位語意對齊屬 #139） |
| §1 L5 形式 B 的「不得假想」＋「兩個乘數以已實現的後果計」 | **兩層判定都是人工的**——沒有機器分得出一段敘述講的是已發生的事故還是假想情境，也沒有機器分得出一段代價敘述講的是**已實際造成的壞狀態**還是**推測的下游災難鏈**。留痕成不成立、後果算不算已實現，全由 PM 讀 |
| §2.7「該線有沒有 `✅通過`／已 merge 未部署的積壓」 | 沒有計數器，**而且它要查的部署狀態欄本身會過期**（§2.1 的 `#78`／`#80`）。刻意不加計數器：一個沒人維護的數字會讓人以為已經在看了 |

> ⚠️ **乘數側的缺口已補，補的是判準不是守衛。** 本檔前一版在此記著「『不得假想』只擋住一半」
> ——它約束事故實例、不約束兩個乘數，於是可以拿真實但無害的小事故過閘、再把代價寫成一條
> 從未發生的災難鏈換取高急迫度。**該缺口已由需求方 2026-08-14 裁定補上**（§1 L5 乘數側的錨：
> 兩個乘數都以已實現的後果計，推測鏈須標示且不計分）。**當時舉的失效實例是 `#139` 自己
> 陳述的代價「兩張卡同時寫同一張生產表」從未發生**——該卡的乘數側論證已依新錨重寫。
>
> ⚠️ **補完之後剩下的限度：「已實際造成」的判定本身仍然是人工的。**
> 「一則 commit 少一行 trailer」與「那一次的整庫備份不存在」之間**沒有機械尺**
> ——兩者都是已實現的**單次**壞狀態（頻率已依裁定四歸再發率，不再混進來當放大器），
> 差的是嚴重度，而嚴重度由 PM 讀。新錨換掉的是**可以憑空寫**（不可證偽），換成
> **要指得出實際存在過的壞狀態**（可證偽、查核者打得穿）；**它沒有換掉「誰在判」**。
> 同一份材料由不同 PM 讀出不同分數，本檔擋不住。**不要把「有錨」讀成「有守衛」。**

> **這三條與上列其餘各條同一個病**：規則寫成敘述性的，就只是敘述性的。
> **不同的是它們新，所以還沒有失效實績**——不要把「還沒被違反」讀成「守得住」。
>
> ⚠️ **但表中第二列（形式 B 的兩層人工判定）自 2026-08-14 裁定四起改變性質**：
> 它不再是「等著被補的病」，而是**刻意不做的事**。下一節逐項說明它為什麼不補、下游由誰承接。
> **其餘各列不受影響**——WIP、Gate、時鐘、母卡目標、checklist 仍然只是敘述性的規則，
> 它們沒有被裁定為刻意，只是還沒有人做。

> 這一段是 R1 finding `CPBL-ROADMAP1-R1-01`／`R1-04` 的正面回應：**規則寫成敘述性的，就只是敘述性的。**
> 要真的機械化需要 `wfcli` 支援規劃狀態、WIP 檢查與停損欄位——那在 ai-workflow 側，見 §7。

### 形式 B 的限度是設計，不是待修的缺陷（需求方 2026-08-14 裁定四）

**本節前一版把下列各點寫成「這條規則的已知漏洞」。定位改寫後它們改記為「這條規則刻意不做的事，
以及為什麼由下游承接」——結論不變，改的是定位。**

> ⚠️ **前一版對這張表的完整性宣稱失實，此處更正**（R6 finding `CPBL-ROADMAP1-R6-001`）。
> 前一版寫「**內容一項不刪**」並把五列宣稱為「五個洞」，**但那五列與執行者 iteration 5
> 實際回報的五點不是同一組**：第 (1) 提出的**具體補法**被淡化成「判斷由 PM 讀」、
> 第 (5)「**因果歸屬未被要求**」**整項沒有對應列**，而表中另有兩列其實來自更早的輪次。
> **後果不是文案瑕疵，是「想過哪些補法、為何不採」無法由本檔重建**
> ——讀者會把「**裁定不採**」讀成「**沒人想到**」。
> 下面改為兩張表：**表 A** 是 iteration 5 原五點的逐點映射（原文為
> [`#130` 留痕補記](https://github.com/ruan6047/cpbl-analytics/issues/130#issuecomment-5291748526)，
> 該留言是完整性的事實來源，本檔只負責可核對地映射過去）；**表 B** 是不屬於那五點、
> 但同樣刻意不做的項。**本檔不再對任何一張表宣稱「一項不刪」。**

#### 表 A：iteration 5 原五點逐點映射

判準連續三輪被收緊，每一輪都找得到新的洞。執行者 iteration 5 回報的五點：

| # | 原文的主張（含它做的區分） | 它提出的具體補法 | 處置 |
|---|---|---|---|
| **(1)** | 「已未遂」那道門讓**控制不存在本身**變成已實現的壞狀態。原文明確區分兩者：**「守衛不存在」**（每一件還沒做的事都恆為真）與**「守衛不存在且真的漏過了一件事」**。並指出裁定殺得掉 trailer 卡，是因為替它選了最窄的描述、替 `#139` 選了較寬的描述——**選描述的人仍然決定分數** | **有，逐字**：「已實現的壞狀態必須是**特定、可指日期的一次實際出錯**，不得是常態性的控制缺席。」 | **需求方裁定不採**，改記為設計的已知限度。⚠️ 留痕中**未見對本補法的逐項評述**；記錄得到的理由只有裁定四的通則（形式 B 是排序判準不是准入閘門，再收一格只會生出下一個判斷題） |
| **(2)** | 描述顆粒度沒有錨（「一則 commit 少 trailer」vs「N 則」、「一份備份是空的」vs「88 天每天一份」），且彙總寫法會與再發率**重複計分**——同一個事實在乘法兩端各算一次。原文並指出裁定自己的 backup 例用持續時間當放大器、trailer 例用單次描述，兩邊顆粒度不同 | 補法即「把顆粒度釘死」 | **五點中唯一被採納的**：裁定四釘為「代價以單次事故實例計、發生頻率一律歸再發率」（§1 L5），`OPS-BACKUP-EMPTY1` 重算後仍排高 |
| **(3)** | 嚴重度沒有尺，**且這不是文風問題**：兩端明顯、中段不明顯。`#139` 的三項與 trailer 的一則客觀上都是「沒造成資料損害、人繞過去了」，裁定給前者中等、後者最低，靠的是**「有沒有擋住別張卡的機械運作」這個沒有寫進錨的隱含判準**——**真正在做工的是它，不是「已實現」三個字** | 無；原文要求的是承認該隱含判準在做工 | **裁定不採**，改記為已知限度。下游：§2.2 三日時鐘 ＋ §2.6——排錯了最多浪費一個 L5 位子三天。⚠️ **該隱含判準至今未寫進錨**，本檔只承認它存在 |
| **(4)** | 結構性張力：本錨對目標 2 的**偵測器類卡同時太鬆與太緊**。偵測器天生「還沒出事就要建」，只能走已未遂那道門，**而那道門正是 (1) 裡定義最寬的門**。收緊到能殺 trailer 會連正當偵測器卡一起殺掉；放寬到能保偵測器，trailer 就活回來——**兩個失效方向由同一個未定義詞控制** | 無；原文明指這是「R6 最該盯的一格」 | **不承接，改為不做**——這一項不是漏洞，**是放棄准入閘門的直接理由**（§1 L5〈形式 B 為什麼不做成准入閘門〉）。留痕載明本點是促成 iteration 6 定位改寫的直接原因 |
| **(5)** | **因果歸屬未被要求。** 錨要求代價是「該事故實例已實際造成的後果」，但**沒有任何一步要求證明因果**。`#139` 引 `#136` 的三項缺口成立是因為兩者真的同源，但規則本身**不擋「把別處的壞狀態掛到自己名下」**——**這正是被駁回那版引 `#136` 序列碰撞的錯誤形狀，換個寫法還是進得來** | 無；缺口的形狀即「須證明因果」 | **裁定不採**，改記為已知限度。**本檔前一版完全沒有對應列，R6-001 據此判失真。** 下游只有「查核者讀得出來」——而本檔記著的三次爛論證中**就有一次正是這個形狀**（§1 的 `#136` 序列碰撞）：**它被讀出來了，但那不是規則擋的** |

> ⚠️ **第 (1) 的補法不是空話，它有明確的作用點——這正是它必須留在檔裡的理由。**
> 原文給了兩個工作例：`#139` 過閘靠的第三項「`#136` 自己的資源宣告從未被檢查過」，
> **那句話的形狀就是「檢查不存在」**；而 `DEV-COMMIT-TRAILER-GUARD1` 只要把已實現後果改寫成
> 「commit trailer 從來沒有機械強制，歸屬欄位在 N 則 commit 上未經驗證」，**本錨一樣殺不掉它**。
> 採用該補法會改變這兩張卡在判準下的讀法。**裁定不採，故本檔維持現行判定**
> （`#139` 中等、trailer 卡最低）——**但「不採」不等於「沒想到」，這一格就是用來擋住那個誤讀的。**

> **判斷題不補、算術題要修**，這是 (2) 與其餘四點的分界：重複計分是**算術錯誤不是判斷歧異**，
> 所以它可以直接修；(1)(3)(4)(5) 全部落在「誰在判」那一側，補判準只會生出下一個判斷題。

#### 表 B：不屬於那五點、但同樣刻意不做的項

| # | 刻意不做的事 | 為什麼不補 | 下游由誰承接 |
|---|---|---|---|
| **B1** | 形式 A／B 在機械面**零接點**——`wfcli open` 強制填的是「服務的原始目標」欄，兩種形式都沒有欄位，也不檢查形式 A 指名的線與卡是否存在 | 欄位語意的窮舉對齊屬 `#139`，**不在本檔射程**；本檔改不了 `wfcli` | §2.7 `open` checklist（人）。⚠️ 其中有一小塊性質不同，見下方保留意見 |
| **B2** | 「事故實例是**已發生**還是**假想**」由 PM 讀 | 沒有機器分得出一段敘述講的是哪一種 | §2.0 規劃 Gate：進 `📥Backlog` 前需求方核可 |
| **B3** | 「代價是**已實現的壞狀態**還是**推測的下游災難鏈**」由 PM 讀 | 新錨換掉的是「可以憑空寫」（不可證偽 → 可證偽），**沒有換掉「誰在判」** | 查核者打得穿——本卡三次爛論證全部是被讀出來的（取樣限制見本節末） |

> ⚠️ **B2／B3 是裁定四明列的兩層人工判定**（本節上方維護表第二列），**不是 iteration 5 的五點**。
> 前一版把 B2 當成第 (1) 的對應列——**那正是 R6-001 指的「淡化」**：(1) 講的不是「判斷是人工的」，
> 而是「**這道門把常態性的控制缺席也算成已實現的壞狀態**」，兩者不是同一件事。

> ⚠️ **執行者的保留意見（非裁定，交查核者判）**：**B1** 裡有一小塊**其實可以機械化**
> ——「形式 A 指名的卡是否存在、是否已是終態」是**參照完整性檢查，不是判斷**：給定 Issue 編號
> 即可查表判真假，與其餘各項的性質不同。它擋不住「指名了一張存在但其實沒被阻塞的卡」，
> 但擋得住指名不存在或已結案的卡。**本檔不自行把它排進去**：欄位面屬 `#139` 射程，
> 且本卡宣告資源只有本檔。此處僅記錄該小塊與其餘各項不同類，**不主張它應該被做**。

**三輪往返本身就是證據，故記錄於此**：iteration 2 補「不得假想」以約束事故實例 → 執行者回報
**只擋住一半**（乘數可憑空寫）→ iteration 3 補乘數側的錨 → 執行者回報**同一個詞往兩邊調都會壞**。
**「下判準 → 找到洞」跑了三輪都沒收斂**，而三次爛論證全部是**人讀出來的**。
**這件事比再補第四條假裝守得住的規則更有資訊量**——所以第四輪改的是定位，不是判準。

> ⚠️ **這份實證的取樣條件不具代表性**（R6 finding `CPBL-ROADMAP1-R6-002`）：
> **三次爛論證全部發生在同一張卡上，而那張卡跑了六輪跨家族查核——一般 L5 卡只有一輪。**
> 所以目前證成的是「**高強度審視下論證藏不住**」；**常態一輪查核下是否同樣藏得住，未經驗證**。
> 這**不推翻**「形式 B 是排序判準而非准入閘門」的定位——把論證寫下來仍嚴格優於不寫——
> 但它是「藏不住就夠了」**目前唯一的實證基礎**，而該基礎的觀測條件不具代表性，
> **讀這條結論時必須連觀測條件一起讀**。

### 維護動作掛在已經會發生的事件上，不另設儀式

單獨的「記得要維護」必然失效；掛在既有觸發點上才有機會發生：

| 已經會發生的事件 | 順手做的維護 |
|---|---|
| `wfcli open`（已機械強制填「服務的原始目標」） | 走 §2.7 的 `open` checklist；確認線別並寫入 `initiative` |
| `wfcli assign` | 走 §2.7 的 `assign` checklist |
| §2.2 逾時重新檢視觸發 | 更新 §3 該列；補記舊卡的線別與母卡目標（§2.5） |
| 卡片結案（`handoff --next-stage release`） | 重跑 §3 的產生指令。⚠️ **這一列目前沒有執行者**，見下方實測註 |
| 引用的外部文件改版 | 見下節 |
| 新事故被引用 | 在 [`incidents/INCIDENTS.md`](incidents/INCIDENTS.md) 建錨點 |

> ⛔ **「卡片結案 → 重跑 §3 的產生指令」這一列，實測是空的。**
> 觀測窗釘死在兩個 SHA 之間：`c62e327`（2026-08-14 18:03 CST，本檔在此之前最後一次被改）
> 到 `f632759`（2026-08-21，`DOC-ROADMAP-STALE-SYNC1` #162 的基線）。**這七天內重跑 0 次**：
>
> ```bash
> git log c62e327..f632759 --oneline -- docs/ROADMAP.md   # → 空輸出（釘死區間，不隨時間變）
> ```
>
> 而同一窗內該觸發點至少發生 **11 次**——區塊裡原有的 43 張活卡有 11 張進入終態、離開活卡集合
> （`DAILY-MIXED-DAY-UX1`、`DATA-BOX-DEEP-SILENT-FAIL1`、`DATA-OFFICIAL-STATUS-TIEBREAK1`、
> `DATA-PA-REBUILD-GAP1`、`DATA-REVISION-HASH-NOISE1`、`DEV-RESOURCE-VOCAB-ALIGN1`、
> `DOC-CPBL-ROADMAP1`、`LIVE-WORKER-RESCHEDULE-FILTER1`、`OPS-SCHEDULE-FAILURE-BLIND1`、
> `UX-GAME-PA1`、`WP-DISCLOSURE-SYNC1`），另有 5 張新卡進入。**區塊因此漂移了七天。**
>
> 依 §0 目標 2 逐字的「**靠人記得看不算**」，本檔**不宣稱這一列已經在運作**。
> ⚠️ **`DOC-ROADMAP-STALE-SYNC1`（#162）只重生了一次區塊，沒有替它建立機械執行者**
> ——該卡的寫入集只有本檔一個檔，建 CI job／git hook／launchd 需要動別的檔，屬另一張卡。
> **所以重生至今仍然靠人記得，而上面那組數字就是「靠人記得」的實測結果；
> 下一次漂移不會有任何東西發出訊號。**

### 引用的維護

§0 劃界表、§1 L3、§4、§5 的外部引用**都釘了 SHA**。**釘 SHA 不會阻止漂移，但會讓漂移可查。**

被引文件更新時：**先確認被引的那一節仍在說同一件事**，再決定沿用或改寫。**不要默默把 SHA 往前推**——那等於宣稱看過而沒看。

### 只有需求方能改的

- **§0 的目標排序。**
- §1 各線的「何時算完」與 L4 的停損要求——**若被證明寫錯，修它比繞過它重要**；但改之前要說明錯在哪，不是因為做不到就放寬。
- §2.0／§2.1／§2.2 的 Gate、WIP 與時鐘常數。

---

## 7. WF 相容升級契約（基線 9）

CPBL 在 WF 暫停期間**可獨立運作**，但必須讓 WF 恢復後接得回去。

### 硬性要求

- 規劃、Gate、WIP、時鐘、停損、清理證明**一律以穩定且版本化的事件 schema 保存**（`cpbl-planning/v1`）
  ——⚠️ **這是要求，不是現況**。`cpbl-planning/v1` 至今**沒有 schema 檔也沒有 writer**（§2.0），
  現況是 Issue 留言。**本節其餘各條都建立在這條之上，故目前一條都還沒真正生效。**
- **WF 恢復後先 shadow comparison**（新舊兩路併行比對），確認一致才停舊 writer
- **禁止雙寫**——兩個 writer 同時寫同一狀態面，衝突時無法判斷誰對
- **禁止把歷史的 `not-asserted` 補成已通過**——沒發生的 Gate 不能事後補造

### capability matrix

每一項能力都要能回答：不變式是什麼、現在怎麼人工做、WF 要具備什麼才能接管、接不回去時怎麼退。

| 能力 | invariant | 現行人工過渡 | WF 原生接管條件 | 回滾 |
|---|---|---|---|---|
| **規劃 Gate**（§2.0） | 未過 Gate 不得進 Backlog／認領 | ⚠️ **僅 Issue 留言**。`planning-started`／`cpbl-planning/v1`／`gate_evidence` **全無實作**（§2.0），故「無證據標 `not-asserted`」目前**無處可標** | ~~`wfcli` **新增** `🧭規劃中` 選項~~ **已於 `ae8f741`（`ai-workflow#102`，2026-08-18）完成**（§2.0 附兩條可重跑取證）；~~剩下的接管條件只有 `open` 不預設 Backlog~~ **亦已於 `1531666`（`ai-workflow#118`，2026-08-21）完成**（`card.py:295` 現值 `"💡需求"`）。⇒ **本列原載的兩項接管條件現皆已達成**，且 `handoff --next-stage backlog` 已對 **T2 以上**課「當下交付狀態必須是 `🧭規劃中`」的前提檢查（`handoff_cmd.py:189/196/488`，`ai-workflow#120`）。⛔ **但「接管」仍不完整，四個已知洞**：（一）`T0`／`T1` 依 `BACKLOG_GATE_EXEMPT_TIERS` 豁免；（二）`handoff --status` 逐字是「覆寫依 next-stage 推導出的交付狀態」（`handoff_cmd.py:397`）；（三）`assign` 於 `assign_cmd.py:255` **無條件**覆寫交付狀態、其 `:227` 只讀**其他**卡的終態；（四）⭐ **`amend --tier` 可先把卡降成 `T1`，再走洞（一）直通**——上游 [`CONTRACT_TOOL_RECONCILE.md`](https://github.com/ruan6047/ai-workflow/blob/main/docs/CONTRACT_TOOL_RECONCILE.md) 逐字載明「級別是 `amend --tier` 可改的欄位，改成 T1 就繞開了這道閘門」。⚠️ **這條路最寬的地方正好是最該擋的那一級**：`card.py:737` 逐字 `REQUESTER_GATED_TIERS = ("T3", "T4")`，而 `tier_downgrade_needs_ruling` 只在原級別屬該集合時要求需求方裁定 ⇒ **`T2`→`T1` 只需 `--reason` 一個字串**，而 backlog gate 正是對 `T2` 以上課的。另 `is_tier_downgrade` 在原級別讀不出時**一律回 `False`**（`card.py:717-725`，刻意設計）。⇒ 原列把接管條件寫成「只剩 `open` 不預設 Backlog」是**不完整的清單**，非僅過期 | 停用 WF 檢查，回到人工 checklist；已寫入的留言保留 |
| **執行 WIP**（§2.1） | 每線同時 ≤1 張佔額度 | PM 於 `assign` 前人工數（§2.7） | `assign` 依線別與狀態集合機械擋下 | 同上 |
| **三日時鐘**（§2.2） | 單一 iteration 逾 3 日觸發重審 | 人工比對 Log 最後認領／退回時間 | `doctor` 能列出逾時卡 | 同上 |
| **L4 停損**（§1） | 無可觀察停止條件不得認領 | PM 於 `assign` 前檢查卡面 | `open`／`assign` 有停損必填欄 | 同上 |
| **`cancelled_with_report`**（§2.6） | 四項證明缺一不可才授權刪除 | 人工逐項寫進 handoff evidence | `cleanup.py` 增此 proof kind | **不回滾**：分支已刪不可逆，故過渡期寧可不刪 |
| **§3 歸屬對帳** | 未歸屬／重複／雙向差集非空即失敗 | **腳本存在且 fail-closed**（`roadmap_lines.py`），**但沒有自動觸發者**——要有人手動跑。⛔ **不得讀成「已機械化」**（實測見 §6〈維護動作掛在已經會發生的事件上〉） | 併入 `doctor`，**且由 `doctor` 擔任觸發者**（只併不觸發等於換個地方沒人跑） | 保留腳本 |

> **本檔引用 WF 的能力契約，不複製其實作細節。** `cleanup.py` 的 proof-kind authority matrix
> 見 `ai-workflow` `71df157`；本檔只承接它的**形狀**（證明決定授權、留痕由實際結果產生、
> 無法證明時保留資源），不抄它的表。

---

## 附錄：以已發生的事故回放檢驗本檔（基線 7）

**分兩軸，不再用「接得住」這個詞。**

- **判準辨識得到**：事故被**人工帶入**之後，本檔的判準能判定「這個狀態不算達成」
- **運行時攔不住**：本檔**不會產生任何訊號**——它是判準文件，不是偵測器

> `0948bae` 版以 ✅「接得住」標示兩起事故，**那個詞暗示已有偵測器**。實際上兩起都只達到
> 「判準辨識得到」。本節依基線 7 改寫。

### `INC-2026-08-10-A`／`-B`｜兩個排程同日失敗，三天無人知

- **判準辨識得到**：✅ §0 目標 2「壞了會被發現；靠人記得看不算」直接否定達成宣稱。§1 L2 的算完狀態同樣不成立
- **運行時攔不住**：❌ 沒有任何訊號。三天後是 PM 為了別的事翻 `launchctl list` 才看到
- **後續**：`OPS-SCHEDULE-FAILURE-BLIND1`（#132）

> 本格曾寫成「`weekly-game-pitches` 排程交付後從未掛上」，**那件事沒有發生**（見 §0 的更正說明）。
> **用一件沒發生的事證明判準有效，比沒有這一格更糟**，故換成實測案例。

### 逐球設備覆蓋告警響了兩個半月無人讀

- **判準辨識得到**：✅ §0 目標 2「告警沒有讀者等於沒有告警」
- **運行時攔不住**：❌ 告警本身是存在的、也正確地響了——**缺的是讀者**，而判準無法產生讀者

### 分項重算誤計 83 筆／82 場

- **判準辨識得到**：⚠️ **部分**。§0 目標 1 要求「對帳或機械檢查擋得住」，而該錯誤確實**最終**被窮舉對帳抓到——但判準**不保證對帳在錯誤上線前跑**
- **運行時攔不住**：❌ 錯誤在線上存在了一段時間
- **缺口**：「有一條可重跑的對帳路徑」與「錯誤不會上線」是兩件事，本檔目前只要求前者。要補這個需要把對帳掛進 CI 或部署前閘門

### `INC-2026-08-13-A`｜`D/97` PA 衍生表未重建

- **判準辨識得到**：❌ **判不出來**。§1 L1 要求「每個數字都有可重跑的對帳路徑」，而**這條對帳根本不存在**——沒有任何機制比較 livelog 與 PA 的產出關係
- **運行時攔不住**：❌
- **本檔能做的只有讓缺口可見**（§1 L1 的警告、§3「已開卡的衍生事故」）

> ⚠️ **這一格的影響面比 `0948bae` 版寫的大**：R1-06 指出 `D/97` 的公開消費者包含賽況頁、
> recap、WP／WPA、關鍵打席與 ΔRE24，**不是純內部資料**。

**這是本檔最誠實的一格**：藍圖是判準，不是偵測器。判準能讓人知道自己沒達成，**不能代替去建那個偵測器**。
