# INGEST-SPLITS-PA-SPLIT1 查證分項重算是否同樣重複計打席〔T4；🔴資料正確性〕

- 需求：ruan6047（2026-07-29 於 `GAME-RECAP-PA1-FIX1` 交付時指示開卡）　規劃：本卡 spec　分支：`ai/<執行者>/INGEST-SPLITS-PA-SPLIT1`
- 執行：待指派（建議 L3；跨模組取捨與官方語意判讀，非新演算法）　查核：待指派（≠ 執行；**跨模型家族或人工**——本卡有統計紅線）
- Initiative：INIT-OFFICIAL-DATA1　spec 基線：—（非 Initiative 子卡，僅歸屬）
- DB：`db_scope: read`（**查證卡；若結論為需重算，另開卡走 `data-migration` 閘門**）
- 部署：否（本卡不改線上資料）　環境：—　PR：—　Merge SHA：—
- 範圍：判定 `splits_calc.py` 是否因與 `GAME-RECAP-PA1-FIX1` 相同的島切分缺陷而重複計打席，若是則量化影響面。**本卡不修**。

## 問題陳述

`GAME-RECAP-PA1-FIX1`（2026-07-29）證實 `game_livelog` 把**打席中途代打換人**記成
`hitter_acnt` 改變，而 `action_name`／`batting_action_name` 是打席層級結果被複製到該打席多列，
因此以 `(inning, half, hitter)` 切島會把**一個打席切成兩個**、同一結果記兩次。

`splits_calc.py` 的 `flush()` 迴圈用的是**同一條切界規則**（`key = (inning, vh, hitter)`），
且它的幽靈島排除只擋「島內無任何 `is_strike`/`is_ball` 列」的情形——擋不住原打者已面對真實投球
才被代打取代的型態。

**這不是推測，切界規則相同是讀碼可確認的事實**；未確認的是它對**官方分項數值**的實際影響。

## 已完成的範圍界定（開卡前實測，全庫 4,276 場）

把 FIX1 認定的 303 對誤切逐一送進 `splits_calc` 的三道過濾（幽靈島 → `batting_action_name`
是否存在 → 是否在 `PA_OUTCOME` 表內），去向是：

- **210 對被幽靈島規則擋掉**（原打者只有牽制／公告列，無真實投球）——**意外地擋對了**
- 6 對因整段無 `batting_action_name` 落入 `skipped_no_outcome`
- **87 對實際被重複計為一個 PA**，散布 **86 場**（2018:9 / 2019:8 / 2020:15 / 2021:5 /
  2022:14 / 2023:16 / 2024:8 / 2025:10 / 2026:2）

被重複記的結果詞前幾名：三振 37、四壞 11、一安 7、投滾 4、游滾 4、右飛 3、中飛 3。

> 這推翻了 FIX1 交付時「303 對可能同樣重複」的初估——實際曝險是 **87**，不是 303。
> 執行者請以 87 為基線，勿沿用 303。

## 主要待驗證假設（本卡的核心，不得跳過）

**H1：打序位移可能比重複計數本身影響更大。** `flush()` 以 `pa_seq` 累加重建打序
（`order = seq % 9 + 1`，因為 livelog `batting_order` 是「該半局第幾位打者」而非打序）。
一個多出來的 PA 會使**該場該隊其後所有 PA 的打序歸屬整體位移 1**，污染的是家族 10
（`ORDER_NAMES`）而不只是那一筆。**此為讀碼推導，尚未實測**——執行者必須先證實或推翻。

**H2：「已與官方對帳過」不構成正確性證明。** `cpbl-verify-splits` 的 harness 註記明寫
「只對『新爬的官方值』有意義（寫回後即自比）」。`cpbl-build-splits` 已**寫回官方表**且
含於每日 refresh，因此當前跑 harness 很可能是拿重算值和重算值比。執行者須確認：
當年定案時的對帳是否涵蓋這 86 場、是否有可重現的當時快照；**若無法重現，就不能宣稱對過帳**。

**H3：官方口徑可能本來就這樣算。** 需查明官方分項對「打席中途代打」計給誰、算幾個打席
（可能牽涉 9.15(b)：代打接替時已兩好球而被三振，該打席記前一位打者）。
先查 [`docs/reference/`](../reference/) 規則庫，**勿從印象推斷**。

## 紅線（違反即退回）

1. **唯讀。** 本卡不得執行 `cpbl-build-splits`、不得寫 `batting_splits`／`pitching_splits`／
   `batting_vs_team`／`pitching_vs_team`／生涯分項，不得動生產。若需重算，回報後另開卡。
2. **不得以「跑了 harness 沒差異」結案**，除非先證明該次 harness 比對的對照組**不是**重算值自身
   （見 H2）。自比通過是零資訊。
3. **完整性宣稱須由 artifact 產生。** 87／86 場等數字與任何「全部／零例外」皆須附產生腳本，
   不得人工聲明。
4. **以棒球規則為前提的判斷須窮盡路徑並附規則書原文出處**（`ML-PITCHER-SCORELESS1/2`
   連兩卡死於規則層列舉不窮盡；見 [[rule-premise-and-reconciliation-limits]]）。
5. **不得順手修 `splits_calc.py`。** 本卡交付的是判定與量化；修正屬另一張卡的範圍，
   混在一起會讓查核無法分辨「查證結論」與「修正正確性」。

## 驗收條件

- [ ] 明確結論：官方已發布的分項數值**是否**因此偏差。三種結論皆為合格交付——
      「有偏差且量化」／「無偏差，並說明為何」／「無法判定，並說明缺什麼證據」。
- [ ] H1 打序位移：以腳本證實或推翻，附受影響的 (場次, 球隊, 位移量) 清單。
- [ ] H2：說明當前 harness 的對照組是什麼，以及當年對帳是否可重現。
- [ ] H3：官方對「打席中途代打」的計算口徑，附規則書或官方資料出處。
- [ ] 若結論為有偏差：量化到選手層級（哪些人的哪些分項欄位差多少），供需求方裁定是否重算。
- [ ] `uv run ruff check` ＋ `uv run pytest` 全綠（本卡若只新增腳本，仍須全綠）。

## 驗證

- [ ] 查核者獨立重跑範圍界定腳本，確認 87／86 場與三道過濾的去向數字。
- [ ] 查核者檢查唯讀紅線：`git diff` 不得觸及 `splits_calc.py` 的計算路徑，
      且執行期間 `batting_splits`／`pitching_splits` 的 `updated_at` 未變動。
- [ ] 查核者自行構造反例挑戰 H1 的推導（打序位移是否真的會傳播到後續 PA）。

## 邊界

- **不做**：修正 `splits_calc.py`、重算或回填任何分項表、動生產。
- **不做**：`GAME-RECAP-PA1-FIX1` 的 canonical PA 表（另卡，已交付待查核）。
  兩卡共用 `continues_same_plate_appearance` 判準，本卡**引用**該判準即可，不重新定義。
- **依賴**：`GAME-RECAP-PA1-FIX1` 查核通過後才動工——若該卡的合併判準在查核中被修改，
  87 這個基線會跟著變，先動工等於白做。
- 預估 S～M。**若查明官方口徑本來就這樣算、或偏差在官方四捨五入內，S 即可收攤。**

## Design Gate

`N/A`——純技術查證卡，無使用者可見介面變更；本卡不改任何線上數值。
若結論導向重算，該卡再依其影響面判斷是否需要 Design Gate。

## Log

- 2026-07-29 依 ruan6047 指示開卡。來源是 `GAME-RECAP-PA1-FIX1` 交付時提報的順帶發現。
  開卡前完成範圍界定實測，把初估的 303 對修正為實際曝險 87 對／86 場——
  `splits_calc` 的幽靈島規則意外擋掉了 210 對。
